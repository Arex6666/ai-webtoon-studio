# 1. 产品定位与商业价值

## 1.1 产品定位

面向 **AI 漫剧工作室/编剧团队/MCN/漫画平台供应链** 的端到端生产工具（SaaS + 私有化两种形态）：

- **Webtoon 长条漫**（竖屏连载）为第一优先级（最商业、最标准化）。
- 支持 Page 分页模式作为后续扩展。

## 1.2 核心卖点（能卖钱的点）

1. **风格一致 & 角色不崩**（角色 Embedding + 风格锁定）
2. **生成即分层**（Character/BG/FG 自动产出 Layer Pack）
3. **一键排版 + 智能嵌字**（气泡不是AI画，文字永不烂）
4. **资产复用**（背景库/表情库/场景空镜库，连载成本指数级下降）
5. **可控可编辑**（画布操作 = 改 JSON；非黑箱；可局部返工）

------

# 2. 总体架构：四层工厂（推荐“强中台 + ComfyUI 集群”）

你可以把系统看成一条流水线：

**Script → Layout(JSON) → Panel(JSON) → Render Layer Pack → Typeset → Compose → Export**

## 2.1 四层架构

### A. 指令结构化层（The Brain）

- 输入：剧情大纲/对白/粗剧本
- 输出：**章节布局 JSON + 分镜 Panel JSON（单一真值）**
- LLM 的角色：**排版导演 + 语义增强 + 连贯性检查**（不是调CFG那种“机械参数”）

### B. 资产一致性层（The Consistency Engine）

- 角色：FaceID Embedding（InsightFace）+ IP-Adapter/InstantID 注入
- 场景：空镜底图 + ControlNet(Depth/Canny/Lineart) 锁结构
- 风格：Style Profile 锁定（模型/LoRA/线条参数/后处理参数）

### C. 执行层（The Layering Factory / ComfyUI Cluster）

核心输出不是“图”，而是**可合成素材包 Layer Pack**：

- `Full.png`（预览）
- `Char.png`（透明人物层）
- `BG.png`（补洞后的完整背景）
- `FG.png`（前景/特效层，可选）
- `Mask.png`（调试/质检用）
- `Meta.json`（层的bounding box、建议视差参数等）

### D. 合成与质检层（Typeset + Compose + QA Loop）

- Typesetter：SVG 气泡 + 中英竖排/横排排版引擎
- Composer：视差、微动（呼吸/镜头缓移/抖动）、导出 MP4/GIF
- QA：抠图白边、补洞露馅、气泡遮脸、风格偏移、角色漂移 → 自动返工/提示人工修正

------

# 3. 核心功能模块（商业工具必须具备）

## 3.1 智能分镜与排版引擎（Layout Engine）——漫剧的灵魂

### 输入

- 剧本/章节文本

### 输出（两类 JSON）

- **Chapter Layout JSON**：章节节奏、长条长度、重点格权重、阅读引导
- **Panel JSON**：每一格的构图、角色、场景、情绪、台词与气泡需求

### 关键能力

- **Panel 权重**：重点格占 2/3 屏 vs 过场 1/3 屏
- **留白预判**：根据台词长度预留气泡空间（不是最终位置，只是候选区域）
- **镜头语言**：景别/透视夸张程度（漫剧张力核心）
- **一致性检查**：天气/服装/道具/场景前后冲突提示（LLM 做“场记”）

> 重要：排版最终不要完全靠 LLM。LLM 提供“意图 + 权重 + 候选约束”，真实布局由算法落地（可控、可重复）。

------

## 3.2 角色与资产库（角色库/场景库/风格库）

### 角色库（Character）

- 上传 3–5 张参考图 → 自动提取 embedding（保存几十 KB）
- 可选：训练 LoRA（当角色服装/发型细节必须固定时）
- 输出：`character_id` → `embedding_path` / `lora_path`

### 场景库（Scene）

- 强推：**空镜底图复用**（连载救命）
- 场景入库：生成/上传一张“完美空镜”，提取 Depth/Canny/Lineart 辅助图
- 输出：`scene_id` → `bg_anchor` + `control_maps`

### 风格库（Style Profile）

锁定“整部作品的画风参数”，例如：

- checkpoint / VAE / LoRA（如 Pony系 + 线稿 LoRA）
- lineart 强度、锐化、色彩后处理
- 默认 Negative Prompt 防御词
- 输出：`style_profile_id`

------

## 3.3 ComfyUI 图层工厂（Layering Pipeline）——你的护城河

核心原则：**生成即分层**，别让用户去 PS。

### 推荐工作流（每个 Panel 一次渲染）

1. **Generate Full**：Pony / 二次元模型 + Lineart ControlNet + IP-Adapter FaceID
2. **Segment**：SAM / RMBG → 得到人物 mask
3. **Extract Char Layer**：人物抠图透明 PNG（边缘羽化/去白边处理）
4. **BG Fill**：LaMa Inpaint 用 mask 补洞 → 得到完整背景
5. **FG（可选）**：抽取光点/雨雪等特效层（或后续由特效生成器提供）
6. **Export Layer Pack**：统一命名与 meta 信息

> 批判性现实：抠图/补洞永远会有低分案例。你必须设计 **“置信度评分 + 最小人工修边工具”**，否则商业交付会卡死。

------

## 3.4 智能嵌字系统（Typesetter）——商业化最容易被低估的模块

**原则：不要让 AI 画气泡。**
 气泡必须矢量化，文字排版必须可控。

### Typesetter 输出

- `panel_composited.png` 或 `panel_layered_with_text.png`
- 以及 `speech.svg`（可编辑）

### 必备能力

- 自动分段换行（中日英混排）
- 竖排支持（古风/日漫）
- 气泡样式库（云朵框/爆炸框/旁白框）
- 气泡候选位置（算法先给 2–3 个建议，用户拖拽微调）

**气泡放置的工程解法（不要靠LLM瞎猜）：**

- 人脸/人体关键点检测 → 生成“禁区”
- OpenCV 显著性/边缘密度 → 找“低信息区”
- 在低信息区内做候选点采样 → 选择冲突最少的点

------

## 3.5 微动合成引擎（Micro-Motion / Parallax Composer）

漫剧不追求大动作，追求“活着的感觉”：

- 视差（BG/Char/FG 不同速度）
- 镜头缓移（Ken Burns）
- 呼吸感（轻微缩放/网格变形）
- 氛围特效（雨雪光斑）可由特效层生成器提供

**SVD 在漫剧里建议只用于：特效层（Atmosphere）**，不要拿它做人物大动作。

------

## 3.6 质检闭环（QA Loop）——决定“能不能商用”

### 自动质检（Vision QA）

- 抠图边缘：白边/断裂/发丝糊
- 背景补洞：鬼影/错纹理
- 角色一致性：脸漂移（embedding 失效或权重不足）
- 气泡遮挡：遮脸/遮关键动作
- 风格漂移：线条变脏、色调跳变

### 自动返工策略

- 轻微问题：换 seed、微调 denoise、加强 negative、增强 lineart
- 结构问题：切换模板（近景对话模板 / 动作夸张模板）
- 抠图失败：切分割模型/参数
- 连续失败：标红 → 人工修边/换背景/改构图

------

# 4. UI/UX：三栏画布的漫剧专用升级（可控、可编辑、不卡）

你之前的画布思路保留，但要强化“漫剧视图”。

## 4.1 三栏结构

- 左：剧本/资产库（角色、场景、风格、气泡样式）
- 中：**Strip View（长条竖屏预览）** + Panel 卡片编辑
- 右：Inspector（图层面板 + 气泡编辑 + 高级参数）

## 4.2 关键交互（避免“节点图陷阱”）

- Panel 卡片为基本单位（不是节点连线）
- 拖拽绑定资产：角色头像拖入 Panel → 绑定 `character_id`
- 图层面板（像 PS）：Char/BG/FG 的位置与视差参数可调
- 乐观 UI + 异步生成：卡片三态（Draft/Generating/Rendered）

## 4.3 单一真值：JSON

- UI 的每一次修改都落到 JSON（可版本化、可回滚、可协作）
- 仅当用户输入“自然语言细节”才调用 LLM 做语义转译

------

# 5. 开发技术栈（建议用这一套，最稳、生态成熟）

下面是“商用可维护”的推荐栈（SaaS 与私有化都好走）。

## 5.1 前端（Web App）

- 框架：**Next.js (React + TypeScript)**
- 画布/编辑：**Konva.js（react-konva）**（做气泡拖拽、图层编辑、蒙版修边）
- 拖拽：dnd-kit
- 状态管理：Zustand（或 Redux Toolkit）
- 富文本：TipTap / ProseMirror（剧本编辑器）
- 实时：WebSocket（任务进度推送）

> React Flow/X6 更适合“节点连线”。你这里是“卡片+画布+图层”，Konva 更匹配。

## 5.2 后端（业务中台）

- API：**Python FastAPI**
- 异步任务：**Celery + Redis**
- 鉴权：JWT + RBAC（团队权限、项目权限）
- 文件/素材存储：S3（云）或 **MinIO（私有化）**
- 关系数据库：**PostgreSQL**
- 向量检索：**pgvector**（够用且部署简单；后期可换 Milvus）
- 搜索（可选）：Elasticsearch / OpenSearch（资产标签多时用）

## 5.3 生成执行层（GPU）

- ComfyUI：多实例集群（每 GPU 一个 Worker）
- 自定义节点：SAM/RMBG/LaMa/Lineart/OpenPose 等节点
- 调度：后端 Job Queue → 分配到某个 ComfyUI Worker
- 通信：HTTP API + Webhook / WS 回传结果

## 5.4 LLM & Vision（智能体）

- LLM：GPT / Claude（用于 Script→JSON、语义增强、连贯性检查）
- Vision QA：GPT-4o Vision（或自研规则 + 小模型组合）
- 预算控制：对 LLM 调用做缓存（同段落同结果）、做批处理与阈值触发（不要每次编辑都调用）

## 5.5 合成与导出

- 视差/微动：Python（OpenCV + FFmpeg）或直接 FFmpeg 滤镜
- 文本排版渲染（关键）：
  - 推荐：**SVG + Headless Chromium（Playwright）渲染成 PNG**（竖排/复杂字体最稳）
  - 或：Pango/Cairo（更底层，但部署复杂）

## 5.6 观测与工程化（商业必备）

- 日志：ELK / Loki
- 监控：Prometheus + Grafana
- 任务追踪：OpenTelemetry
- 成本计量：每次生成记录 GPU 秒数、LLM token、存储与带宽

------

# 6. 最佳开发路径（MVP → V1 → V2），保证“能交付、能卖”

## MVP（最快变现）

**目标：静态 Webtoon 长条漫 + 智能嵌字 + 角色一致性**

- Script→Panel JSON
- 角色 embedding 注入（不崩脸）
- 场景空镜复用（不崩背景）
- 气泡系统（SVG + 可拖拽编辑）
- 导出：长条 PNG（或分格 PNG）

> 这版就已经能对接“内容生产团队”，开始收费。

## V1（护城河形成）

**目标：自动 Layer Pack + 视差微动导出 MP4**

- ComfyUI 分层流水线稳定（Char/BG/FG）
- Parallax + 呼吸感 + 镜头缓移
- QA：抠图/补洞/遮挡评分 + 低分提示修边

## V2（连载工业化）

**目标：资产银行 + 自动返工闭环 + 团队协作**

- 背景库、表情库、动作姿势库（静态关键姿势）
- Vision QA 自动重试（换 seed / 切模板 / 局部重绘）
- 多人协作、版本树、A/B、批量生成、配额与计费

------

# 7. 你必须提前接受的“商业现实”（我直接帮你踩刹车）

1. **抠图/补洞无法 100% 全自动**：必须提供最小可用的“修边工具”，否则会卡死交付。
2. **风格一致性靠“锁配置”而不是“让用户自由调参”**：自由度太高=产能下降。
3. **排版与嵌字是核心竞争力**：很多AI工具死在“字很丑、气泡乱放”。
4. **连载的关键是资产复用**：背景库/表情库做好，你的成本会比同行低一个数量级。

------

# 8. 最终交付形态（你对外可以这样包装）

- **AI 漫剧工作台（Studio）**：从剧本到长条漫/动效漫一键出稿
- 输出：
  - Webtoon 长条 PNG / 分格 PNG
  - Motion Comic MP4（视差微动 + 气泡 + 特效层）
  - 工程包（Layer Pack + JSON + 可复用资产）