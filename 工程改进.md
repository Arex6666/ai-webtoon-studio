# 路线A（工业化交付）任务包 A-B01 ~ A-B08

## 任务包 A-B01：Chapter Bundle 交付规格 v1（先定“产品协议”）

### 目标

定义你要交付给“下游制作/发行/审核/二次加工”的标准包，让后面所有模块围绕它工作。

### 要做什么

- 定义 bundle 目录结构（推荐 v1 标准）：

  ```
  chapter_bundle.zip
  ├─ manifest.json                 # 总清单（权威入口）
  ├─ chapter.json                  # 章节元数据（标题/作者/版本/时间）
  ├─ panels/
  │  ├─ 0001/
  │  │  ├─ panel.json              # 本格镜头元数据（PanelSpec快照）
  │  │  ├─ layerpack/
  │  │  │  ├─ manifest.json         # LayerPack Manifest v1
  │  │  │  ├─ full.png
  │  │  │  ├─ char.png (optional)
  │  │  │  ├─ bg.png (optional)
  │  │  │  ├─ fg.png (optional)
  │  │  │  ├─ char_mask.png (optional)
  │  │  ├─ typeset/
  │  │  │  ├─ bubbles.json          # 气泡数据快照
  │  │  │  ├─ typeset.png           # 带字成品
  │  │  ├─ qa.json                  # QA结果（可选）
  │  ├─ 0002/...
  ├─ strip/
  │  ├─ strip.png (optional)        # 可选：同时打包长条漫
  ├─ provenance/
  │  ├─ jobs.json                   # 本章所有 job 运行记录
  │  ├─ assets.json                 # 引用的资产版本锁定清单
  │  ├─ workflows/                  # workflow模板快照（可选）
  └─ README.txt                     # 使用说明（可选）
  ```

- 定义 `manifest.json`（总入口）字段：

  - chapter_id / chapter_version
  - panels[]（顺序、每格路径、类型、时长占位）
  - produced_at
  - bundle_hash（可选）
  - min_required_viewer_version（可选）

### 产出物

- `docs/bundle_spec_v1.md`
- JSON schema（前端 Zod + 后端 Pydantic 对齐）

### 验收标准

- 任意人拿到 zip，不用看数据库也能还原：每格是什么、产物在哪里、用什么参数生成

### 依赖模块

- 无（这是契约）

### 风险点

- 一开始字段别太复杂：先确保“可读、可追溯、可复现”，再加“成本统计/版权信息”等

------

## 任务包 A-B02：LayerPack Manifest v1 “权威化”（让分层资产有科技含量）

### 目标

把 LayerPack 从“几张图”升级成“可复现资产包”。

### 要做什么

- LayerPack manifest 必须包含：
  - geometry：width/height, safe_area, dpi（可选）
  - files：full/bg/char/fg + masks/alpha（有则列）
  - provenance：model_name+hash、workflow_name+hash、seed、steps/cfg/sampler、prompt_pack（序列化）
  - inputs：引用资产（face_embedding_id、scene_anchor_id、control_maps）
  - quality：qa_score、issues、needs_fix
  - deterministic_key：用于幂等（比如 hash(prompt+seed+model+workflow+size)）
- 后端 layerpacks 服务：写入/读取都以 manifest 为主

### 产出物

- `apps/api/.../schemas/layerpack_manifest.py`
- `apps/web/.../schema/layerpackManifest.ts`

### 验收标准

- 任意 LayerPack 只靠 manifest 就能重跑生成（至少能构造同样的 payload）

### 依赖模块

- layerpacks.py / services/layer_factory

### 风险点

- 不要把大图 base64 塞 manifest，只存 URL/相对路径

------

## 任务包 A-B03：Chapter ExportJob（BundleJob）接口与状态机

### 目标

把“打包”做成一个正式 Job，可排队、可追踪、可重试。

### 要做什么

- `POST /api/v1/exports/{chapter_id}/bundle` → 返回 jobId
- Job 输出：
  - bundle_url（MinIO）
  - manifest_url（MinIO 单独存一份，方便快速预览）
  - stats：panel_count / total_size / qa_summary（可选）
- WS 推送：
  - export_started / export_progress / export_done / export_failed

### 产出物

- exports.py 增加 bundle 入口
- jobs.py 支持 ExportBundleJob 类型

### 验收标准

- 前端点一下“Build Bundle”，能看到进度，最终拿到 zip 下载链接

### 依赖模块

- jobs / exports / ws / minio

### 风险点

- zip 生成要在 worker 跑，别阻塞 API

------

## 任务包 A-B04：Bundle Builder（真正的打包流水线）

### 目标

实现“从 DB 权威数据 → 拉取产物 → 组装目录 → zip → 上传 MinIO”。

### 要做什么（打包步骤）

1. 读取 chapter + panels 顺序（DB 权威）
2. 对每个 panel：
   - 选定一个“导出使用的 LayerPack”（规则：latest_success 或 user_selected）
   - 下载/拷贝 LayerPack 文件（full/bg/char…）
   - 导出 panel.json（PanelSpec 快照）
   - 导出 bubbles.json（如果有）
   - 导出 typeset.png（如果有）否则 fallback full.png
   - 导出 qa.json（如果有）
3. 生成 chapter.json、provenance/jobs.json、assets.json
4. 生成总 manifest.json
5. 目录写到临时目录 → zip
6. 上传 MinIO（bundle_url）
7. 写 DB：Export 记录、job.outputs

### 产出物

- `services/export/bundle_builder.py`（或你已有 export service 里新增）
- 统一的 `resolve_panel_export_artifact()` 选择逻辑

### 验收标准

- 任何章节（哪怕只有 1 个 panel）都能打包成功
- zip 解压后结构完全符合 A-B01 spec
- typeset 缺失时仍能打包（降级策略明确）

### 依赖模块

- exports service / layerpacks service / minio

### 风险点

- 大章（上百格）要注意内存：用流式写入、边下边写、不要全读进内存

------

## 任务包 A-B05：前端“Build Bundle”产品化（不是一个按钮，而是一个工序）

### 目标

让 Studio 像“生产控制台”，让人感觉这是工业系统。

### 要做什么

- Chapter 顶部工具栏新增：
  - Build Bundle 按钮
  - 最近一次 bundle 的状态、大小、时间
  - 下载入口（zip + manifest）
- Export Console：
  - 显示每个 panel 的打包进度（可选）
  - 失败时定位到具体 panel（例如 “panel 0007 缺 full.png”）

### 产出物

- StudioTopbar + Bottom Job Console 增强
- Export 页面/弹窗（可选）

### 验收标准

- 非技术用户也能看懂：打包在做什么，失败在哪里，怎么修

### 依赖模块

- exports API + WS job 事件

### 风险点

- UI 不要一次上全功能，先把“可见进度 + 可下载”做稳

------

## 任务包 A-B06：Bundle 的“资产锁定与可复现”（科技含量核心）

### 目标

让 bundle 具备“复刻能力”，这就是你和普通“生成几张图”的区别。

### 要做什么

- `assets.json` 里记录：
  - 每个引用资产的 version_id / hash / source_url
  - 模型文件（checkpoint、lora、embedding）的引用清单
- `provenance/jobs.json` 里记录：
  - jobId、attempt、provider、耗时、参数摘要、输出文件清单
- 可选：把 workflow 模板快照也打包进 `provenance/workflows/`

### 产出物

- provenance 收集器（bundle_builder 内）
- 资产解析器（从 LayerPack manifest + job inputs 反推引用资产）

### 验收标准

- 你把 zip 给另一个环境：只要模型资源齐全，就能按 provenance 重跑出同样产物（至少参数一致）

### 依赖模块

- layerpacks manifest + jobs outputs + assets versions

### 风险点

- “hash”必须可靠：模型文件、workflow 模板都要有 hash，否则锁不住

------

## 任务包 A-B07：质量门禁（QA Gate）与 NeedsFix 接入导出

### 目标

让导出变成“生产门禁”，不合格就不允许出包（或出包标红）。

### 要做什么

- 导出前做 Gate：
  - 必须存在可用产物（typeset 或 full）
  - 分辨率一致
  - QA score 未达标 → 标记 chapter needs_fix
- Bundle manifest 写入：
  - qa_summary（min/avg、失败列表）
  - needs_fix_panels[]

### 产出物

- export gate 校验器
- UI：导出前弹出 QA 汇总（可选）

### 验收标准

- 故意缺图/坏图 → 导出失败并指出具体 panel
- QA 不达标 → 允许“强制导出（带警告）”或“禁止导出”二选一（由你产品策略决定）

### 依赖模块

- qa.py / FixPlan / LayerPack meta

### 风险点

- Gate 规则要可配置（阈值别写死）

------

## 任务包 A-B08：Bundle Viewer / 快速预览（让交付包能“即开即看”）

### 目标

你交付 zip 后，下游不想解压一堆文件才知道内容。

### 要做什么

- 生成 `manifest_preview.json` 或直接让 `manifest.json` 支持：
  - panels 的缩略图列表（thumb_url）
- 后端提供：
  - `GET /exports/{export_id}/manifest`（快速预览）
- 前端提供：
  - “Bundle Preview” 页面：列表浏览每格（缩略图+文字）

### 产出物

- exports manifest API
- 前端 preview 页面

### 验收标准

- 不下载 zip 也能看章内容，下载只是为了交付/归档

### 依赖模块

- exports / minio

### 风险点

- 缩略图别在导出时现算太慢：可复用 full 的小图或在生成阶段就生成 thumb

# Bundle v1（工业版）任务包 A-B2-01 ~ A-B2-10

## A-B2-01：Bundle Spec v1 最终定稿（目录+字段+JSON Schema）

### 做什么

- 固定 zip 目录结构（必须包含：panels/*/layerpack、typeset、provenance、assets.json、jobs.json）
- 固定 4 个核心 JSON：
  1. `manifest.json`（总入口）
  2. `chapter.json`（章节元数据）
  3. `panels/xxxx/panel.json`（PanelSpec 快照）
  4. `panels/xxxx/layerpack/manifest.json`（LayerPack Manifest v1）
- 为 `manifest.json` 和 `layerpack/manifest.json` 写 schema（后端 Pydantic + 前端 Zod 同步）

### 产出物

- `docs/bundle_spec_v1.md`
- `apps/api/app/schemas/bundle_manifest.py`
- `apps/web/src/lib/schema/bundleManifest.ts`
- `apps/api/app/schemas/layerpack_manifest.py`（若还没权威化）

### 验收

- 任意 zip 只看 `manifest.json` 就能定位所有文件路径与引用关系（不依赖数据库）

------

## A-B2-02：LayerPack Manifest 权威化（可复现 + 输入/产出完整）

### 做什么

把 LayerPack manifest 变成“权威复现描述”，必须包含：

**(1) provenance（复现核心）**

- model：checkpoint/lora/embedding 的 name + hash + version_id
- workflow：template_name + template_hash + injected_params（扁平化）
- run：seed/steps/cfg/sampler/scheduler/size/dtype
- prompt_pack：positive/negative/styleProfileSnapshot（序列化）

**(2) inputs（依赖资产引用）**

- face_embedding_id / character_id（若有）
- scene_anchor_id / control_maps（若有）
- init_image_url / reference_images（若有）

**(3) files（产物清单）**

- full.png 必须
- char.png/bg.png/fg.png 可选
- char_mask.png/alpha.png 可选
- thumb.jpg 可选（推荐）

**(4) geometry（给 typesetter/导出用）**

- width/height
- safe_area（上/下/左/右）
- bboxes：角色框/人脸框（可选，后期做）

### 产出物

- manifest 生成器（后端服务层）
- layerpacks.py：创建/读取时以 manifest 为准

### 验收

- 只要有 manifest + 资源文件，就能在另一台机器上“参数一致地重跑”（至少能重构 payload）

------

## A-B2-03：ExportBundleJob 类型落地（状态机+WS+重试策略）

### 做什么

- 在 jobs 系统里新增 job_type：`EXPORT_BUNDLE`
- 统一状态：QUEUED/RUNNING/SUCCEEDED/FAILED/NEEDS_FIX
- WS 事件必须包含：
  - 当前阶段（collecting → fetching → packaging → uploading → done）
  - 当前 panel index / total panels（进度条）
  - 当前失败原因（具体到 panel 与缺失文件）

### 产出物

- jobs.py：新增类型与 outputs schema
- ws.py：新增 export bundle 的事件推送

### 验收

- 前端能实时看到“导出到第几格”，失败能定位是哪一格缺什么

------

## A-B2-04：BundleBuilder 核心流水线（收集→拉取→组装→zip→上传）

### 做什么

实现 bundle_builder 的“工业流程”，建议拆成 6 个函数（强烈建议这样写，后面可测试）：

1. `collect_chapter_snapshot(chapter_id)`

- 读 DB：chapter + panels 顺序 + timeline（可选）
- 输出：chapter_snapshot（纯 JSON）

1. `resolve_panel_artifacts(panel_id)`

- 决定导出用哪个 LayerPack（规则：user_selected > latest_success）
- 决定 typeset 用哪个（latest_success）
- 输出：artifact_plan（文件 URL 列表 + manifest URL）

1. `fetch_files_to_staging(artifact_plan, staging_dir)`

- 从 MinIO/HTTP 拉到本地 staging（流式下载）
- 缺文件直接抛明确错误（给 QA gate）

1. `write_panel_folder(panel_idx, staging_dir, panel_snapshot, layerpack_manifest, bubbles_snapshot, qa_snapshot)`

- 写入 panels/0001/... 的所有 JSON
- 拷贝 layerpack 文件到 panels/0001/layerpack/
- 拷贝 typeset.png & bubbles.json 到 panels/0001/typeset/

1. `write_bundle_root_files(staging_dir, manifest, chapter_json, assets_json, jobs_json)`

- 生成并写入根目录 JSON（manifest/chapter/assets/provenance/jobs）

1. `zip_and_upload(staging_dir) -> bundle_url`

- zip（不要把全部读内存）
- 上传 MinIO
- 返回 bundle_url + manifest_url

### 产出物

- `apps/api/app/services/export/bundle_builder.py`
- `apps/api/app/services/export/bundle_errors.py`（错误类型：MissingArtifact/CorruptManifest/UploadFail）

### 验收

- 1章 20格：稳定生成 zip，解压结构符合 spec
- 任意缺文件：报错信息能明确指出“panel 0007 缺 full.png”

------

## A-B2-05：Assets Lock（assets.json）实现：引用资产版本锁死

### 做什么

在 bundle 里写 `assets.json`，把“本次作品依赖的所有资产版本”锁死：

- 模型文件：checkpoint/vae/clip/lora/embedding（name+hash+version_id+storage_key）
- 角色相关：FaceEmbedding（id+hash+source）
- 场景相关：SceneAnchor/Control maps（id+hash+storage_key）
- 字体：typesetter 字体包（font_name+font_version+license_tag）

实现方式：

- 从 **LayerPack manifest.inputs/provenance** 汇总依赖
- 从 **jobs.json** 再补一层（防止漏）

### 产出物

- `apps/api/app/services/export/asset_lock_resolver.py`

### 验收

- zip 里的 assets.json 能回答：“这章用到了哪些模型文件与资产版本”
- 资产 hash 变化会导致 deterministic_key 变化（避免误复用）

------

## A-B2-06：Provenance（provenance/jobs.json）实现：可追溯运行历史

### 做什么

生成 `provenance/jobs.json`，至少包含：

- job_id / job_type / provider / created_at / finished_at / duration_ms
- attempts[]：每次 attempt 的 error / params / outputs
- 关键摘要：seed、workflow_name、model_hash、prompt_hash

数据来源：

- RenderJob/RenderAttempt 表
- LayerPack manifest.provenance（交叉验证一致性）

### 产出物

- `apps/api/app/services/export/provenance_collector.py`

### 验收

- 你能靠 jobs.json 复盘：为什么这一格变了？是哪次 attempt 产出的？

------

## A-B2-07：Typeset 快照入包（bubbles.json + typeset.png + 字体信息）

### 做什么

导出时每格必须具备：

- `typeset/typeset.png`（最终可读成品）
- `typeset/bubbles.json`（编辑数据快照）
- bubbles.json 中必须包含：fontFamily/fontSize/strokeWidth/align/wrapMode

同时，typesetter 输出应写入 meta：

- 字体实际使用的 fallback（防止跨机器不一致）

### 产出物

- typeset 服务输出结构补强（若已有则对齐 spec）
- bundle_builder 集成 typeset 快照

### 验收

- 解压 zip，用 typeset.png 拼 strip 都没问题
- bubbles.json 能回灌到 Studio（可编辑）

------

## A-B2-08：Export QA Gate（导出门禁 + needs_fix_panels 列表）

### 做什么

打包前做导出门禁：

- 每格必须有可用图：优先 typeset.png，否则 full.png
- 分辨率一致（width/height 必须匹配章节 spec）
- LayerPack manifest 必须可解析且包含 full.png
- QA（若存在）低于阈值：列入 needs_fix_panels，但**仍允许导出**（工业系统常用“带黄牌交付”）

把结果写入：

- `manifest.json.qa_summary`
- `manifest.json.needs_fix_panels[]`

### 产出物

- `apps/api/app/services/export/export_gate.py`

### 验收

- 缺关键文件直接失败（红牌）
- 低分 QA 只是标记（黄牌），zip 仍能出但 manifest 里清楚列出

------

## A-B2-09：前端 Build Bundle 工序化（可视化进度 + 错误定位）

### 做什么

前端要做到“像生产系统”：

- Chapter 顶部：Build Bundle 按钮 + 最近一次 bundle 记录（时间/大小/状态）
- 底部 Job Console：展示 export job 阶段与进度
- 失败时：显示 “panel 0007 缺 typeset.png → 点击定位到该 panel”

### 产出物

- StudioTopbar + JobConsole 增强
- export 记录查询 UI（可简单列表）

### 验收

- 不看后端日志，用户也能知道为什么失败、该改哪一格

------

## A-B2-10：Bundle Preview（不用下载也能看章内容）

### 做什么

- 后端提供 `GET /exports/{id}/manifest`（从 MinIO 读 manifest）
- 前端提供 Bundle Preview 页面：
  - 左侧面板列表（0001..）
  - 右侧预览：typeset.png/全图
  - 显示 QA 标记与 needs_fix

### 产出物

- exports API 增强
- 前端 preview 页面/弹窗

### 验收

- 生成 bundle 后，前端可立即预览，不必下载 zip

------

# A-B2-04（文件级任务清单）

## 1) 新增/调整文件清单（你要改哪些文件）

### 必改

1. `apps/api/app/services/export/bundle_builder.py` ✅（新增核心）
2. `apps/api/app/services/export/bundle_errors.py` ✅（新增错误类型）
3. `apps/api/app/services/export/bundle_models.py` ✅（新增内部数据结构）
4. `apps/api/app/api/routes/exports.py` ✅（新增接口触发 bundle job）
5. `apps/api/app/workers/export_bundle.py` ✅（新增 Celery 任务）
6. `apps/api/app/services/export/export_gate.py`（可先 stub）⚠️（门禁先做基础）
7. `apps/api/app/services/export/provenance_collector.py`（可先 stub）⚠️
8. `apps/api/app/services/export/asset_lock_resolver.py`（可先 stub）⚠️

### 可能需要补一个存储工具（如果你没有统一封装）

1. `apps/api/app/services/storage/object_store.py`（MinIO/S3 下载+上传封装）

------

# 2) bundle_models.py：内部数据结构（先写“骨架类型”）

**文件：** `apps/api/app/services/export/bundle_models.py`

你要定义几个 dataclass/Pydantic（内部用 dataclass 就行）：

### (1) ChapterSnapshot

包含导出所需最小信息：

- chapter_id, title, order, style_profile_snapshot（可选）
- panels: List[PanelSnapshot]

### (2) PanelSnapshot

- panel_id, index (0001…), panel_spec_json（存快照）
- selected_layerpack_id（可为空）
- selected_typeset_id（可为空）

### (3) PanelArtifactPlan

“这一格需要拉哪些文件到 staging”：

- panel_index_str：`"0001"`
- layerpack_manifest_url / layerpack_files: List[(storage_key, dest_relpath)]
- typeset_png_url（可空）+ bubbles_json（可空）
- qa_json_url（可空）

### (4) BundleBuildResult

- staging_dir
- bundle_local_path
- bundle_url
- manifest_url
- total_bytes
- panel_count

**验收**：这些结构能让 bundle_builder 写起来不乱。

------

# 3) bundle_errors.py：错误类型（让失败可定位）

**文件：** `apps/api/app/services/export/bundle_errors.py`

定义异常类（至少这些）：

- `BundleBuildError(code, message, details)`
- `MissingArtifactError(panel_index, artifact_name, expected_path_or_key)`
- `CorruptManifestError(panel_index, manifest_url, reason)`
- `DownloadError(url_or_key, reason)`
- `UploadError(target_key, reason)`
- `GateRejectedError(reason, panel_index=None)`

**验收**：错误能被上层捕获并转成 job failure + WS 提示。

------

# 4) object_store.py：MinIO/S3 统一拉取与上传（如果你已有可跳过）

**文件：** `apps/api/app/services/storage/object_store.py`

必须提供 4 个方法（同步/异步随你工程风格）：

1. `download_to_file(url_or_key, local_path) -> None`（流式）
2. `upload_file(local_path, target_key, content_type) -> public_url`
3. `exists(url_or_key) -> bool`
4. `open_stream(url_or_key) -> BinaryIO`（可选）

**验收**：能在 bundle_builder 中统一用，不散落 boto3/minio 客户端。

------

# 5) bundle_builder.py：核心 6 步函数（逐个实现）

**文件：** `apps/api/app/services/export/bundle_builder.py`

建议你按下面函数签名写（名字可以不同，但结构别变）：

## 5.1 collect_chapter_snapshot(chapter_id) -> ChapterSnapshot

### 做什么

- 查询 DB：Chapter + Panels（顺序字段）
- 每个 panel 取 `panel_spec` 的“导出快照”（别引用草稿指针，直接 dump json）
- 可先不管 timeline（bundle v1 可后补）

### 关键点

- panel 顺序必须稳定（order/index）
- `panel_index_str = str(i+1).zfill(4)`

### 验收

- chapter_snapshot.panels 数量正确，panel_spec_json 完整

------

## 5.2 resolve_panel_artifacts(panel_snapshot) -> PanelArtifactPlan

### 做什么

决定“导出用哪个 LayerPack / Typeset / QA”：

- LayerPack 选择规则（必须实现）：
  1. panel.selected_layerpack_id（用户选）优先
  2. 否则取 latest_success layerpack（按 created_at）
- Typeset 选择规则：
  1. latest_success typeset（如果你 typeset 有 job/attempt）
  2. 没有则为空（后面降级用 full.png）
- QA 选择规则：有就带上，没有就空

### 需要你依赖的 DB 查询（你已有模型就直接用）

- LayerPack 表：panel_id + status=SUCCEEDED
- Typeset 输出表（如果没有单独表，就从 job outputs 查）

### 验收

- 每个 panel 都能返回一个 artifact_plan
- 计划里至少包含 layerpack_manifest_url 或 layerpack_id（后续会拉 manifest）

------

## 5.3 fetch_files_to_staging(plan, staging_dir) -> None

### 做什么

- 创建目录：
  - `panels/0001/layerpack/`
  - `panels/0001/typeset/`
- 下载以下文件到 staging：
  - `layerpack/manifest.json`（必须）
  - manifest 里列出的 files（至少 full.png 必须）
  - typeset.png / bubbles.json（可选）
  - qa.json（可选）

### 关键点（工业化）

- **先下载 manifest，再解析文件列表**
- manifest 解析失败 → `CorruptManifestError`
- full.png 缺失 → `MissingArtifactError`
- 全部用流式下载，避免大章 OOM

### 验收

- staging_dir 下能看到 panel 目录结构与文件落盘

------

## 5.4 write_panel_folder(panel_snapshot, panel_index_str, staging_dir, parsed_layerpack_manifest, bubbles_snapshot, qa_snapshot) -> None

### 做什么

在 `panels/0001/` 写入三类文件：

- `panel.json`：PanelSpec 快照（必须）
- `layerpack/manifest.json`：写入已下载的（必须）
- `typeset/bubbles.json`：如果有（可选）
- `qa.json`：如果有（可选）

### 关键点

- panel.json 里要额外写 2 个字段（建议）：
  - `export_selected_layerpack_id`
  - `export_selected_typeset_id`

### 验收

- 解压 zip 后，单独打开 panels/0001/panel.json 能看懂这格是什么

------

## 5.5 write_bundle_root_files(staging_dir, chapter_snapshot, panel_plans, provenance, assets_lock) -> None

### 做什么

写根目录：

- `chapter.json`：chapter 元信息 + panel_count + spec_version
- `manifest.json`（总入口）：
  - spec_version
  - chapter_id / chapter_version（如果有）
  - panels[]：每格路径、预览图路径（typeset.png 优先）、needs_fix 标记（后面 gate 填）
  - generated_at
- `assets.json`：来自 assets_lock_resolver（先 stub 也行，但字段要有）
- `provenance/jobs.json`：来自 provenance_collector（先 stub 也行，但字段要有）

### 验收

- 打开 manifest.json 能顺着路径找到每格 typeset.png/full.png

------

## 5.6 zip_and_upload(staging_dir, chapter_id, export_id) -> (bundle_url, manifest_url, size_bytes)

### 做什么

- zip 文件名建议：`chapter_{chapter_id}_export_{export_id}.zip`
- 同时把 `manifest.json` 单独上传一个（方便前端 preview）
- 上传到 MinIO：
  - `exports/{chapter_id}/{export_id}/chapter_bundle.zip`
  - `exports/{chapter_id}/{export_id}/manifest.json`

### 验收

- 返回两个 URL：bundle_url 与 manifest_url
- bundle 可下载解压

------

# 6) workers/export_bundle.py：Celery 任务（把 builder 串起来）

**文件：** `apps/api/app/workers/export_bundle.py`

### 做什么

- 输入：chapter_id, export_id, job_id
- 执行顺序：
  1. WS: stage=collecting
  2. snapshot = collect_chapter_snapshot
  3. 对每格：
     - WS: stage=fetching, progress=i/n
     - plan = resolve_panel_artifacts
     - fetch_files_to_staging
     - write_panel_folder
  4. WS: stage=packaging
  5. provenance/assets（先 stub -> 返回空结构但 schema 对）
  6. write_bundle_root_files
  7. WS: stage=uploading
  8. zip_and_upload
  9. 更新 Export 表 / Job outputs
  10. WS: done

### 验收

- 一次导出全过程 WS 事件完整
- 失败时 job 进入 FAILED，且 message 能定位到 panel/文件

------

# 7) routes/exports.py：新增接口（创建 Export + 创建 Job）

**文件：** `apps/api/app/api/routes/exports.py`

### 做什么

新增：

- `POST /api/v1/exports/{chapter_id}/bundle`
   返回：
- export_id
- job_id
- status（QUEUED）

创建逻辑：

1. DB 创建 Export 记录（type=BUNDLE，status=QUEUED）
2. DB 创建 Job 记录（type=EXPORT_BUNDLE）
3. 投递 Celery：export_bundle.delay(chapter_id, export_id, job_id)
4. 返回

### 验收

- 前端一调接口就能看到 job 进入队列

------

# 8) export_gate.py（先做基础门禁，不要复杂）

**文件：** `apps/api/app/services/export/export_gate.py`

### 做什么（v1 最小）

- 对每 panel：
  - layerpack manifest 能解析
  - full.png 存在
  - typeset.png 可选（没有就降级）
- 写入 gate 结果：
  - `manifest.needs_fix_panels`（先空）
  - `manifest.qa_summary`（先空）

### 验收

- 缺 full.png → 直接拒绝导出（GateRejectedError）

------

# 9) 前端配合点（你现在就可以安排）

你前端已经有 Job Console，导出要补：

- StudioTopbar 加 “Build Bundle”
- 点击后调 API 创建 job
- Job Console 显示 export job 的 stage/progress
- 完成后展示两个链接：
  - 下载 zip
  - 预览 manifest（后续 A-B2-10）

------

# 10) 最重要的“验收脚本”（你要强制自己做）

导出成功后你必须手动检查：

1. zip 解压结构完全符合 spec
2. panels/0001/typeset/typeset.png 能打开
3. panels/0001/layerpack/manifest.json 存在且字段齐全
4. 根目录 manifest.json 能串起来所有路径
5. assets.json/jobs.json 存在（哪怕先是空结构，也要 schema 完整）

## 你接下来应该干什么（按优先级）

### 1) 先把 ExportBundle 的“发动机”写出来（A-B2-04 核心）

目标：后端能在 Worker 里完成 **收集 → 拉取 → 组装目录 → zip → 上传 → 写回 DB**。

你要做的事情只有一件：
 **实现 BundleBuilder 的 6 步函数**（就算 assets/provenance/qa 先留空也没关系，但目录与流程必须闭环）。

验收标准：

- 同一章导出 3 次都能成功
- zip 大小合理、包含每格最少一个可用图
- 任何缺文件都能报“panel 0007 缺 full.png”这种精确错误

------

### 2) 把 Job/WS 做成“生产感”的工序（A-B2-03）

目标：导出不是黑盒，用户能看到阶段和进度。

你要做的事情：

- 新增 `EXPORT_BUNDLE` job_type
- WS 推送：stage + progress（i/n）+ 当前 panel_index
- 前端 Job Console 能显示导出进度

验收标准：

- 导出时前端进度条从 0 → 100%
- 失败能定位到具体 panel

------

### 3) 把“选择哪个产物导出”规则固定（resolve 规则）

目标：工业化的关键是“选择逻辑可解释”。

你要做的事情：

- LayerPack 选择规则（用户选 > latest_success）
- Typeset 选择规则（有就用，没有就降级 full）

验收标准：

- 改一格后再导出，能明确导出的是哪次成功产物

------

### 4) 做一个最小 Gate（导出门禁）

目标：防止导出“看似成功但其实缺图”的伪成功。

你要做的事情：

- 每格必须存在可用图（typeset 或 full）
- full 缺失直接失败
- 分辨率不一致直接失败（避免 strip 合成炸掉）

验收标准：

- 缺关键文件绝不出包
- 出包必可拼条漫

------

### 5) 再做“科技含量”两件套：可复现与资产锁（A-B2-05/06）

这两块是你项目真正“像中台”的核心。

你要做的事情：

- assets lock：这章依赖了哪些 checkpoint/lora/embedding/font（版本+hash）
- provenance：导出时记录 job/attempt 摘要（seed/workflow/model）

验收标准：

- 任何一格的结果都能解释“它怎么来的”
- 将来你做批量生产/回滚/复跑才不会失控