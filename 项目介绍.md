# AI Webtoon Studio

**AI 漫剧智能创作工作台 — 从创意到成片，一站式 AI 驱动**

---

## 产品定位

AI Webtoon Studio 是一款面向漫剧创作者、内容工作室和 MCN 机构的**工业级 AI 漫剧生产工具**。

核心理念：**Canva 的易用性 × Midjourney 的生成力 × CapCut 的剪辑逻辑**

只需输入一个创意，系统自动完成"剧本→分镜→画面→排版→配音→成片"全流程，将传统数周的漫画制作周期**压缩至数小时**。

---

## 核心能力

### 🧠 AI 对话式创作

- **灵感即产品**：输入一句话创意，AI 自动生成完整策划大纲
- **智能体协作**：内置编剧、导演、美术总监、质检等 10 个专业 AI 智能体协同工作
- **对话式调整**：通过自然语言对话修改剧情、角色、画面，无需任何专业知识
- **多集连载**：支持多集自动规划与批量生成

### 🎬 智能分镜引擎

- **三阶段 AI 流水线**：剧本解析 → 分镜规划 → 资产绑定，全程自动化
- **4 种导演风格预设**：默认 / 浪漫 / 惊悚 / 沉思，适配不同题材
- **智能 QA**：自动检测构图、角色、场景问题并给出修复建议
- **草稿预览**：生成后可预览、微调、部分采用

### 🎨 多引擎图像生成

- **3 个生成引擎**：ComfyUI（本地部署）、豆包 Seedream、通义万相
- **角色一致性**：FaceID Embedding + IP-Adapter 保证同一角色跨镜头不走样
- **场景一致性**：Depth / Canny / Lineart 控制图锁定场景结构
- **分层输出**：每张图自动产出角色层、背景层、前景层、蒙版层（LayerPack）
- **智能重试**：生成不达标时自动换种子、调参数、局部修复

### ✍️ 专业排版系统

- **SVG 矢量气泡**：5 种气泡样式（对话 / 思考 / 喊叫 / 低语 / 旁白）
- **智能定位**：AI 推荐气泡位置，避免遮挡关键画面
- **可视化编辑**：拖拽调整位置、大小、字体、颜色

### 🎙️ AI 配音与音乐

- **角色配音**：为每个角色绑定 AI 音色，自动对白配音（豆包 TTS）
- **口型同步**：配音自动生成口型动画数据
- **字幕生成**：自动生成时间轴字幕
- **AI 配乐**：根据场景氛围自动生成背景音乐

### 📹 视频与导出

- **条漫 PNG**：标准竖屏长条漫（Webtoon 格式）
- **微动视频**：视差动画 + 镜头缓移 + 呼吸感
- **多引擎视频**：豆包 / 通义 / ComfyUI 视频生成
- **工业化 Bundle**：含 Manifest、来源追溯、质量报告的标准化交付包

---

## 三大工作面

| 工作面 | 定位 | 核心交互 |
|--------|------|----------|
| **🏠 首页** | 灵感入口 | 创意输入框 + 6 大题材模板 + 灵感广场 |
| **📋 策划工作台** | AI 对话驱动 | 剧集管理 + Agent 对话 + 资产锁定门禁 |
| **📹 制作工作台** | 时间线编辑 | 多轨时间线 + 画布预览 + 素材面板 |

另有**传统 Studio 编辑器**，提供分镜列表、画布编辑、图层管理、参数面板等专业功能。

---

## 技术架构

```
┌────────────────────────────────────────────────┐
│                  前端 (Next.js)                 │
│  首页 · 策划工作台 · 制作工作台 · Studio 编辑器   │
└──────────────────────┬─────────────────────────┘
                       │ REST + WebSocket
┌──────────────────────▼─────────────────────────┐
│                后端 API (FastAPI)                │
│  42 个 API 模块 · 37 个数据模型 · JWT 认证       │
├─────────────┬─────────────┬────────────────────┤
│  AI 智能体  │  对话系统   │   LLM Brain        │
│  (10 个)    │  (编排器)   │   (4 Provider)     │
├─────────────┴─────────────┴────────────────────┤
│              服务层 (可插拔 Provider)             │
│  图像工厂 · 视频引擎 · 语音合成 · 排版 · QA      │
└──────┬──────────┬──────────┬───────────────────┘
       │          │          │
  ┌────▼───┐ ┌───▼────┐ ┌──▼──────┐
  │ComfyUI │ │ 豆包   │ │ 通义    │
  │(本地)  │ │(云API) │ │(云API)  │
  └────────┘ └────────┘ └─────────┘
```

**基础设施**：PostgreSQL + Redis + MinIO（对象存储）

---

## 数据安全与可控性

- ✅ **支持私有化部署**：全部代码自主可控，可部署在客户自有服务器
- ✅ **数据隔离**：项目间数据完全隔离，支持多用户权限管理
- ✅ **Provider 可选**：LLM / 图像 / 视频引擎均可按需切换，不锁定供应商
- ✅ **全链路可追溯**：每张图、每个视频片段均可追溯到生成参数与资产版本
- ✅ **版本管理**：支持版本快照与回滚

---

## 产品亮点

| 亮点 | 说明 |
|------|------|
| **角色不崩** | FaceID Embedding + IP-Adapter 保证跨镜头角色一致性 |
| **生成即分层** | 自动产出角色/背景/前景分层素材，便于后期编辑 |
| **文字不烂** | SVG 矢量气泡 + 程序化排版，告别 AI 生成的"乱码文字" |
| **资产复用** | 背景库/角色库/风格库全项目复用，连载成本指数级下降 |
| **可控可编辑** | 画面 = JSON 数据，非黑箱，任意局部可返工 |
| **全自动闭环** | 质量不达标自动重试，无需人工干预 |

---

## 适用场景

- 📱 **Webtoon 竖屏长条漫** — 连载漫画的高效生产
- 🎬 **Motion Comic 动效漫** — 视差微动 + 配音 + 配乐的视频内容
- 📖 **IP 快速可视化** — 小说/剧本 → 视觉化 Demo
- 🎓 **短剧/广告脚本** — 分镜预览与快速制作
- 🏢 **MCN 内容工厂** — 批量化、标准化的内容生产流水线
