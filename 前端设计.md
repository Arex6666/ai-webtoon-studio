## 1) 你的前端到底要做什么（一句话版）

**一个“章节工作台 Studio”**：把剧本 → 分镜（PanelSpec）→ 资产（角色/场景/风格）→ 渲染任务（ComfyUI/视频大模型）→ 质量检查 → 返工 → 嵌字 → 导出，串成一个可编辑、可追踪、可批量生产的流水线 UI。

------

## 2) 你图里的流程，对应前端要落哪几个“核心域”

你上传的流程图里最关键的是这条状态机：

**Draft → Queued → Running → NeedsFix → Rendered → TypesetDone → Exported**

前端要做的不是“生成图片/视频”，而是：

1. **让用户编辑 Draft**（剧本/分镜/资产/气泡/镜头/时长）
2. **把 Draft 变成可执行的 Render Payload**（PanelSpec JSON + style profile + asset refs）
3. **把任务送进队列**（RenderJobs）
4. **实时看到任务跑到哪了**（WebSocket 推送 Running/进度/失败原因/QA分）
5. **NeedsFix 能定位到“哪个镜头/哪个层/哪个气泡/哪个资产”出了问题**，并提供“局部重绘/重试/换模型/降级”的入口
6. **Rendered 输出 LayerPack**（Full / Char / BG / FG + meta）
7. **TypesetDone 把气泡渲染到图上**（SVG→PNG）
8. **Exported 导出长条漫 PNG / 微动 MP4 / 全片 MP4**

------

## 3) “Flux 静态图分层”是干什么的？放在哪？

### 它是什么

你说的「Flux 静态图分层」本质上是：
**用 FLUX 出一张“可工业化复用”的分层结果**（LayerPack），而不是一张“不可控的一次性成图”。

典型 LayerPack 包含：

- `Full.png`：合成后的完整画面（给预览/交付）
- `Char.png`：人物抠图层（透明背景，可用于一致性修复/微动/后续视频）
- `BG.png`：背景层（可复用、可修补）
- `FG.png`：前景特效层（雨丝、光斑、雾气等，可选）
- `Mask.png / Alpha.png`：分割/抠图中间产物（用于局部重绘）
- `meta.json`：这个包对应的 PanelSpec、风格、种子、模型、版本、生成时间、QA评分等

### 它放在哪（前端视角）

- **资产库（Assets）**：作为某个 Panel 的渲染产物归档（可复用）
- **Panel 详情页 / Studio 右侧 Inspector**：以 “LayerPack Viewer” 形式查看、开关层、替换层、触发局部重绘
- **Exporter（导出页）**：把 LayerPack 批量合成成长条漫或 MP4

所以：**Flux 静态分层是你工业化的地基**，它让“后续视频/修复/微动/嵌字/复用”都有抓手。

------

## 4) 前端信息架构（页面路由 + 导航）

> 我按 Next.js App Router 设计（你现在就是 Next.js + TS + Tailwind + Konva）。

### 路由建议（最小但可扩展）

- `/` → Dashboard（项目列表、最近打开、队列概览）
- `/projects` → 项目列表
- `/projects/[projectId]` → 项目总览（章节、资产、风格模板、导出记录）
- `/projects/[projectId]/chapters/[chapterId]` → 章节总览（分镜列表、状态统计）
- `/projects/[projectId]/chapters/[chapterId]/studio` → **核心：章节工作台**
- `/projects/[projectId]/assets` → 资产库（角色/场景/风格/LayerPack）
- `/jobs` → 全局渲染队列（管理员/批量监控）
- `/exports/[exportId]` → 导出详情（日志、下载、失败原因）

------

## 5) Studio（章节工作台）的“可用级”布局拆分

你说你已经有“四区布局”，我建议把四区明确成 **四个“域面板”**，每个面板都能独立演进：

### A. 左侧：Script & ToFix（输入与问题）

- 剧本编辑器（段落→镜头映射）
- LLM 分镜生成按钮（生成/重写/扩写）
- NeedsFix 列表（按镜头聚合）
- 资产快捷引用（角色/场景/风格模板）

### B. 中央：Storyboard + Panel Cards（分镜总控）

- Panel 卡片网格/列表（缩略图 + 状态 + QA分 + 时长）
- 选中某个 Panel 后，中央可切换：
  - **画面预览（Full）**
  - **分层预览（LayerPack）**
  - **时间轴预览（微动/镜头轨）**（V2）

### C. 右侧：Inspector（当前镜头的“精修面板”）

- PanelSpec 表单（人物/场景/天气/画风/运镜/时长）
- 气泡列表（每个 bubble 的文本/位置/样式）
- Render 参数（模型/分辨率/seed/cfg/采样器/视频模型）
- 局部重绘（仅重绘人物/背景/气泡区域）

### D. 底部：Jobs Console（任务与日志）

- 当前章节队列（Queued/Running/失败重试）
- 实时日志（WebSocket）
- 一键批量渲染（选中N个 Panel → Render）

------

## 6) 前端“数据模型”要先定死（否则 UI 会乱长）

你后端已经有 projects/chapters/panels/assets/render/typeset/exports/jobs/ws，前端必须对应一套强类型模型（TS + zod）。

### 建议的核心类型（精简但够用）

```ts
// Project / Chapter
export type Project = { id: string; name: string; createdAt: string; updatedAt: string };
export type Chapter = { id: string; projectId: string; title: string; order: number; status: ChapterStatus };

// Panel（分镜）
export type PanelStatus = "Draft" | "Queued" | "Running" | "NeedsFix" | "Rendered" | "TypesetDone" | "Exported";
export type Panel = {
  id: string;
  chapterId: string;
  order: number;
  status: PanelStatus;
  durationSec?: number;       // 给视频/微动用
  scriptSpan?: { start: number; end: number }; // 映射到剧本段落
  panelSpec: PanelSpec;       // 关键：可编辑源数据
  latestLayerPackId?: string; // 渲染产物
  qa?: { score: number; issues: string[] };
};

// LayerPack（分层产物）
export type LayerPack = {
  id: string;
  panelId: string;
  fullUrl: string;
  charUrl?: string;
  bgUrl?: string;
  fgUrl?: string;
  metaUrl: string;
  createdAt: string;
};

// RenderJob（执行记录）
export type RenderJob = {
  id: string;
  panelId: string;
  status: "Queued" | "Running" | "Succeeded" | "Failed";
  progress?: number; // 0..1
  provider: "comfyui" | "keling" | "tongyi" | "doubao";
  error?: string;
  createdAt: string;
};
```

### PanelSpec（你系统的“生产指令”）

PanelSpec 是你整个工业化的核心。建议先做 v0（够渲染 + 可扩展）：

```ts
export type PanelSpec = {
  styleProfileId: string;       // 风格模板
  sceneId: string;              // 场景库引用
  characters: Array<{
    characterId: string;        // 角色库引用（FaceID/LoRA/Embedding）
    pose?: string;              // 可选：姿态模板id
    expression?: string;        // 表情模板id
    placement: { x: number; y: number; scale: number; z: number };
  }>;
  camera: {
    shot: "WS"|"MS"|"CU"|"ECU"; // 景别
    angle?: "eye"|"high"|"low";
    movement?: "static"|"push_in"|"pull_out"|"pan"|"tilt";
  };
  env: {
    weather?: "light_rain"|"rain"|"fog"|"sunny"|"night";
    time?: "day"|"dusk"|"night";
  };
  prompt: {
    positive: string;
    negative?: string;
  };
  bubbles: Array<{
    id: string;
    text: string;
    style: { fontSize: number; fontFamily?: string; shape: "round"|"rect"|"thought" };
    transform: { x: number; y: number; w: number; h: number; rotate?: number };
    tail?: { x: number; y: number }; // 指向说话者
  }>;
  render: {
    engine: "flux"|"sdxl"|"illustration";
    width: number;
    height: number;
    seed?: number;
    steps?: number;
    cfg?: number;
  };
  video?: {
    targetSec?: number;         // 未来扩展：首尾帧/关键帧
    provider?: "keling"|"tongyi"|"doubao";
  };
};
```

> 注意：你“要商业化”，PanelSpec 必须是**可追踪、可复现、可回滚**的“生产指令”。前端要支持版本化（V2再做也行，但结构先留口子）。

------

## 7) 状态管理：React Query + Zustand 的分工要分清

### 用 React Query 管“后端事实”

- projects/chapters/panels/assets/jobs/exports 列表与详情
- mutations：create/update panelSpec、submit render job、retry job、typeset、export

### 用 Zustand 管“编辑器瞬时状态”

- 当前选中的 panelId
- PanelSpec 编辑草稿（dirty/undo/redo）
- Konva 画布选中对象、拖拽状态
- 右侧 Inspector 折叠面板状态
- Studio 的视图模式（storyboard / preview / layers / timeline）

**核心原则**：
“PanelSpec 的源数据最终落库”走 React Query mutation；
“正在编辑但未保存的内容”放 Zustand（支持撤销/保存提示）。

------

## 8) WebSocket：前端必须实现“任务实时感知”

你后端已经有 `ws.py`，前端建议统一成：

- `WS /ws` 建立连接
- 订阅：chapterId / panelIds / jobIds
- 推送事件：job_progress / job_status / panel_status / qa_result / export_status

### 事件结构建议（前端好处理）

```ts
export type WsEvent =
  | { type: "job_progress"; jobId: string; panelId: string; progress: number }
  | { type: "job_status"; jobId: string; panelId: string; status: "Queued"|"Running"|"Succeeded"|"Failed"; error?: string }
  | { type: "panel_status"; panelId: string; status: PanelStatus }
  | { type: "qa_result"; panelId: string; score: number; issues: string[] }
  | { type: "export_status"; exportId: string; status: "Running"|"Succeeded"|"Failed"; error?: string };
```

前端接到事件后做两件事：

1. **更新 React Query cache**（invalidate 或 setQueryData）
2. 若当前正在编辑某 panel，**提示冲突**（例如“后台产物已更新，是否刷新预览？”）

------

# 9) 现在开始：我给你“可直接喂给 AI 编程工具”的任务包（按顺序做）

下面每个任务包，你都可以整体复制给你的 AI 编程工具执行。**不要跳步**，跳步会导致后面返工很大。

------

## 任务包 01：前端工程骨架与依赖（必须先做）

```txt
✅ 任务包01：前端工程骨架与依赖（超细拆分执行指令）
目标（必须达成）

Next.js App Router + TS + Tailwind ✅

React Query ✅

Zustand ✅

Zod + react-hook-form ✅

react-resizable-panels ✅

shadcn/ui ✅（至少：Button / Dialog / Sheet / Tabs / Table / Badge / DropdownMenu / Tooltip / Toast）

统一 API client（baseUrl、token、错误处理）✅

QueryClientProvider + Toaster + 全局 Layout（左侧导航 + 主内容区）✅

最终目录与关键文件落地 ✅

/dashboard 页面可访问，显示空壳 Dashboard ✅

A) 初始化与依赖安装

你的 AI 编程工具执行以下操作（按顺序）：

创建 Next.js 项目（如果你还没创建）

项目名建议：webtoon-studio-frontend

使用 App Router、TS、Tailwind、ESLint

安装依赖（务必一次装齐）

@tanstack/react-query

zustand

zod

react-hook-form

@hookform/resolvers

react-resizable-panels

clsx

tailwind-merge

lucide-react

安装并初始化 shadcn/ui

使用 Tailwind

生成 components/ui/*

生成 shadcn 组件（至少这些）

button

dialog

sheet

tabs

table

badge

dropdown-menu

tooltip

toast

（可选但推荐）separator、scroll-area、input、textarea

B) 目录结构（必须按这个结构落地）

在 src/ 下（如果你用 src 目录）：

src/
  app/
    layout.tsx
    globals.css
    dashboard/
      page.tsx
  components/
    app/
      AppShell.tsx
      SideNav.tsx
      TopBar.tsx
  lib/
    api/
      client.ts
      endpoints.ts
    query/
      QueryProvider.tsx
    store/
      studioStore.ts
    utils/
      env.ts
      cn.ts


如果你的工程不是 src/ 结构，就直接在根目录按同名路径创建（不要纠结，只要结构一致）。

C) 关键文件内容（逐文件写死）
1) src/lib/utils/env.ts

要求：

统一读取环境变量

baseURL 默认 http://localhost:8000（你 FastAPI）

可在 .env.local 覆盖

内容要求（必须包含）：

API_BASE_URL

WS_BASE_URL（可先占位）

2) src/lib/utils/cn.ts

要求：

封装 cn()（clsx + tailwind-merge）

shadcn 官方写法即可

3) src/lib/api/endpoints.ts

要求：

统一写 endpoint 常量（先留空也行）

至少包含：

projects

chapters

panels

renderJobs

exports

4) src/lib/api/client.ts

要求：实现一个最小但可扩展的 typed fetch wrapper
必须支持：

baseURL

bearer token（先从 localStorage 或 cookie 占位）

JSON 请求/响应

统一错误格式（throw Error）

对 401/403 提供 hooks（先 console.warn 也行）

必须导出：

apiGet<T>(path, opts?)

apiPost<T>(path, body, opts?)

apiPatch<T>(path, body, opts?)

apiDelete<T>(path, opts?)

5) src/lib/query/QueryProvider.tsx

要求：

QueryClient

默认 retry 规则（失败不疯狂重试）

包裹 children

6) src/lib/store/studioStore.ts

要求：

Zustand store 空壳（先不做业务）

先定义字段：

selectedPanelId: string | null

setSelectedPanelId(id)

isDirty: boolean

setDirty(bool)

reset()

7) src/components/app/SideNav.tsx

要求：

左侧导航 UI

至少两个入口：Dashboard、Projects（先写死链接）

使用 shadcn Button 或简单链接都行

当前路由高亮（usePathname）

8) src/components/app/TopBar.tsx

要求：

顶部栏显示标题（例如 Webtoon Studio）

右侧预留用户/设置入口（占位）

9) src/components/app/AppShell.tsx

要求：

页面整体布局：左侧固定导航 + 右侧主内容

主内容区域顶部 TopBar

children 渲染在 main 内

使用 Tailwind 做基础样式

10) src/app/layout.tsx

要求：

引入 globals.css

包裹 QueryProvider

包裹 shadcn Toaster

使用 AppShell 包裹页面内容

设置 metadata title

11) src/app/dashboard/page.tsx

要求：

返回一个简单页面：

标题：Dashboard

一行说明：Project overview coming soon

作为运行验收页

D) 样式要求（Tailwind / shadcn）

页面背景浅灰、卡片白色、边框淡

侧边栏宽度固定（例如 w-56）

主内容区可滚动

E) 验收标准（AI 工具执行完你要看到的结果）

yarn dev 启动无报错

打开 http://localhost:3000/dashboard 能看到：

左侧 SideNav

顶部 TopBar

主区域 Dashboard 标题

浏览器控制台无红色致命错误
```

------

## 任务包 02：路由与导航框架（Dashboard/Projects/Project/Chapter/Studio）

```txt
✅ 任务包02：路由与 Studio 四区布局（超细拆分）
目标（必须达成）

路由体系：/projects → /projects/[projectId] → /projects/[projectId]/chapters/[chapterId]/studio ✅

Studio 页面四区布局（你描述的“四区布局”）：左侧脚本/资产、中间故事板+画布、右侧 Inspector（Story/Cast/Layout/Layers）、底部时间轴/相机/音频（占位） ✅

可拖拽分割（react-resizable-panels）✅

Zustand + URL 状态联动（选中 panelId 写进 query）✅

Storyboard 列表 + Panel Cards 占位（可点击选中）✅

Canvas 区域占位（后续 Konva/Preview）✅

右侧 Tabs（Story/Cast/Layout/Layers）✅

顶部 Studio 工具条：保存、渲染、导出（占位按钮）✅

A) 新增目录结构（必须按此落地）

在 src/ 下新增：

src/
  app/
    projects/
      page.tsx
      [projectId]/
        page.tsx
        chapters/
          [chapterId]/
            studio/
              page.tsx

  components/
    studio/
      StudioShell.tsx
      StudioTopbar.tsx
      StudioLayout.tsx

      left/
        ScriptEditor.tsx
        AssetBrowser.tsx

      center/
        StoryboardView.tsx
        PanelCard.tsx
        CanvasStage.tsx

      right/
        InspectorTabs.tsx
        InspectorStory.tsx
        InspectorCast.tsx
        InspectorLayout.tsx
        InspectorLayers.tsx

      bottom/
        TimelineDock.tsx

B) 路由页面内容（先做“可用壳子”，后续再接 API）
1) /projects 页面：src/app/projects/page.tsx

要求：

显示 Projects 列表（先 mock 3 个）

每个项目卡片点击进入 /projects/[projectId]

2) /projects/[projectId] 页面：src/app/projects/[projectId]/page.tsx

要求：

展示 projectId

展示章节列表（mock 3 个 chapters）

点击某章节进入 /projects/[projectId]/chapters/[chapterId]/studio

3) Studio 页面：src/app/projects/[projectId]/chapters/[chapterId]/studio/page.tsx

要求：

读取 params projectId/chapterId

读取 query：panelId（可为空）

渲染 <StudioShell projectId chapterId />

C) StudioShell 组成与布局
1) StudioShell.tsx

职责：

管“顶层 context”

渲染：Topbar + StudioLayout

从 URL query 读取 panelId 并写入 store（首次同步）

当 store.selectedPanelId 改变时，更新 URL query（保持同步）

保证不会无限循环（需做对比判断）

D) 四区布局（核心）

使用 react-resizable-panels 实现：

顶部固定 Topbar（48~56px）

主体区域：左右三栏（Left / Center / Right）可拖拽

底部 Dock（Timeline）高度可拖拽（或先固定 180px，下一包再加拖拽）

建议比例：

Left：320px

Center：自适应

Right：360px

Bottom：200px

E) 中区细分：Storyboard + Canvas

Center 区分上下两部分：

上：Storyboard（可滚动的 Panel Cards 列表，横向或纵向都可以，推荐纵向列表）

下：CanvasStage 占位（以后接 Konva/Preview）

要求：

PanelCard 可点击选中

选中态高亮

每个 PanelCard 显示：Panel #、一句摘要、状态 Badge（Draft/Rendered/NeedsFix 占位）

F) 左区：ScriptEditor + AssetBrowser（先占位但要像工作台）

Left 区分上下 Tab 或上下分区（推荐 Tabs）：

Tab1：Script（一个 textarea，后续接剧本）

Tab2：Assets（一个 list：Characters / Scenes / Styles 三组占位）

G) 右区：Inspector Tabs

右区必须 Tabs：

Story：显示当前 panel 的“镜头信息占位”（shot type, camera move, weather, time, mood）

Cast：显示角色占位（林知夏 / 周屿）

Layout：显示 PanelSpec 占位（画幅、分镜布局）

Layers：显示图层占位（BG/Char/FG/Text）

要求：

如果没有选中 panelId，显示引导：“请选择一个分镜面板”

H) 底部：TimelineDock

先做“可用占位”：

顶部显示：Duration、FPS、Clip count（mock）

下方显示一个时间轴条（div）

右侧有 “生成微动/导出MP4（占位）” 按钮

I) 状态管理（Zustand 扩展）

在 src/lib/store/studioStore.ts 扩展字段：

projectId: string | null

chapterId: string | null

panelList: { id: string; index: number; title: string; status: 'Draft'|'Rendered'|'NeedsFix' }[]

setContext(projectId, chapterId)

setPanelList(list)

selectPanel(id)

selectedPanelId 保留

isDirty 保留

Studio 进入时：

setContext

setPanelList(mock 25 个 panels（你之前提到 25）)

J) UI 组件使用要求（统一风格）

用 shadcn：Button / Tabs / Badge / ScrollArea / Separator / Input/Textarea

Storyboard 列表要可滚动

三栏布局高度适配窗口（不要溢出）

Studio 页面整体：h-[calc(100vh-...)] 或用 flex

K) 验收标准（必须都能做到）

yarn dev 无报错

/projects 可进、可点项目

/projects/[id] 可点章节进入 studio

studio 四区布局完整显示

拖拽分割条可调整宽度（至少左右三栏）

点击 PanelCard 会高亮，并且 URL 变成 ?panelId=xxx

刷新页面后 panelId 仍保持选中状态

右侧 Inspector 根据是否选中显示不同内容
```

------

## 任务包 03：API 合约与类型（zod + typed fetch）

```txt
✅ 任务包03：PanelSpec 数据模型 + 可编辑 Inspector + 本地草稿保存（超细拆分）
目标（必须达成）

每个 Panel 有一份 PanelSpec（结构化 JSON）✅

右侧 Inspector 四个 Tab 变成“可编辑表单”，支持修改并实时反映到 Store ✅

面板切换：自动加载对应 PanelSpec 到表单 ✅

修改后：Panel 变为 isDirty=true，并显示 “未保存/草稿”标记 ✅

自动草稿保存：按 projectId+chapterId 维度保存到 localStorage（带 debounce）✅

支持导入/导出 JSON（导出当前 chapter 的 panelspec 集合；导入覆盖）✅

Storyboard 卡片展示：标题/摘要/状态/时长/天气等关键信息（从 PanelSpec 派生）✅

A) 先定义 PanelSpec（强类型 + Zod 校验）
新增文件
src/lib/schema/panelSpec.ts
src/lib/schema/chapterSpec.ts
src/lib/schema/index.ts

1) PanelSpec 必须包含的字段（第一版够用）

id: string（panelId）

index: number（序号）

shot:

shotType: "WS"|"MS"|"CU"|"ECU"|"OTS"|"POV"|"Establishing"

cameraMove: "static"|"pan"|"tilt"|"dolly_in"|"dolly_out"|"truck"|"handheld"|"zoom"

durationSec: number（0.5~15，先限制）

lensHint: string（可空）

scene:

location: string（旧书店门口/咖啡店靠窗等）

timeOfDay: "day"|"night"|"dusk"|"dawn"|"indoor"

weather: "clear"|"rain"|"snow"|"fog"|"overcast"

mood: string（温柔/克制/心动…）

characters: string[]（引用角色 assetId / characterId）

dialogue:

lines: Array<{ speaker: string; text: string; type: "speech"|"narration"|"thought" }>

style:

styleProfileId: string | null（未来接你的 style profile）

negativePrompt: string（可空）

render:

status: "Draft"|"Queued"|"Running"|"Rendered"|"NeedsFix"

lastRenderAt: string | null

warnings: string[]

meta:

createdAt: string

updatedAt: string

2) 用 Zod 做 schema + 默认值 factory

panelSpecSchema

createDefaultPanelSpec(panelId, index)：返回全默认 PanelSpec

B) ChapterSpec（把一个章节的 panelspec 集合打包）

新增 chapterSpecSchema：

projectId

chapterId

version

panels: PanelSpec[]

用于导出/导入。

C) 扩展 studioStore：面板数据从“list”升级为“specMap”

在 src/lib/store/studioStore.ts 增加：

必加字段

panelSpecs: Record<string, PanelSpec>

selectedPanelId: string|null（已有）

setPanelSpec(panelId, patch)：局部更新（深合并）

replacePanelSpec(panelId, spec)：整替换

loadChapterDraft(projectId, chapterId)：从 localStorage 加载

saveChapterDraft()：存 localStorage（序列化 chapterSpec）

exportChapterSpec()：返回 JSON string

importChapterSpec(json)：校验通过则覆盖 panelSpecs

必加派生方法（写成 store 的 helper）

getSelectedSpec()：返回当前选中 PanelSpec

getPanelSummary(panelId)：从 spec 派生摘要（标题/时长/天气/地点/角色数）

markDirty(panelId, bool)：你可以放到 render.status + meta.updatedAt 里体现

注意：不要在 store 里存 react-hook-form 的状态，表单只负责“编辑”，store 才是“真相数据源”。

D) 本地草稿持久化：localStorage（按章节维度）
Key 规则

ai-webtoon:chapterDraft:${projectId}:${chapterId}

策略

StudioShell 初始化时调用 loadChapterDraft(projectId,chapterId)

如果无草稿：用 mock 25 panels 自动生成默认 PanelSpec（createDefaultPanelSpec）

任何 panelSpec 变更：debounce 600ms 自动 saveChapterDraft()

Topbar 上加按钮：

“保存草稿”（立即 save）

“导出JSON”

“导入JSON”（文件上传/粘贴两种都行，先做粘贴 Modal 也行）

E) Inspector 表单：react-hook-form + zodResolver
需要依赖（如果你还没装）

react-hook-form

@hookform/resolvers

zod

新增组件
src/components/studio/right/InspectorFormProvider.tsx

设计原则（非常重要）

每次切换 panel：form.reset(selectedPanelSpec)

表单编辑时：onChange 立即 patch 到 store（或用 debounce patch）

patch 的粒度：只 patch 改动路径，例如 shot.durationSec、scene.weather

四个 Tab 的编辑内容（第一版先够用）
1) Story Tab（镜头）

shotType（Select）

cameraMove（Select）

durationSec（Slider + Input）

lensHint（Input）

mood（Input）

旁白/氛围（可放在 dialogue 里 type=narration 或单独 field，你先放 dialogue 里也行）

2) Cast Tab（角色）

多选 characters（先用 mock 角色列表：林知夏、周屿）

每个角色可显示 tags（发型/衣服占位）

后续你会接向量库/LoRA，但这一包先把 UI 和数据结构跑通

3) Layout Tab（画面与分镜布局占位）

location（Input）

timeOfDay（Select）

weather（Select）

画幅 aspectRatio（先存到 meta 或 scene，二选一；建议放 meta.frame 以后扩展，这一包先简化不强制）

4) Layers Tab（分层渲染参数占位）

render.status（只读显示 + 小按钮“标记NeedsFix”“重置Draft”）

warnings（list，可编辑增删）

negativePrompt（Textarea）

styleProfileId（Select，占位：default / rainy_soft / cinematic）

F) Storyboard 卡片增强：从 PanelSpec 派生显示

更新 PanelCard.tsx：

标题：scene.location + shot.shotType（例如：旧书店门口 / Establishing）

Subtitle：weather + timeOfDay + durationSec

Badge：render.status

Dirty 标记：如果 render.status === Draft 或 meta.updatedAt > lastRenderAt（简化）

点击：selectPanel + 更新 URL（你任务包02已经做了）

G) 导入/导出 JSON（必须能用）
导出

Topbar 点击 “导出JSON”

直接下载文件 chapter-${chapterId}-spec.json（用 Blob + a 标签）

导入

Topbar 点击 “导入JSON”

弹出 Modal（shadcn Dialog）

支持粘贴 JSON

点击确认：

zod 校验

成功：覆盖 store.panelSpecs + save draft

失败：toast 显示错误

H) 交互细节（避免踩坑）

切换 panel 时：先 form.reset(spec) 再渲染各 Tab

表单 onChange 写 store：要防抖，否则输入框会卡

推荐：输入框本地即时更新（react-hook-form），同时用 useEffect(watch) + debounce patch store

store -> form 回写：只有在 selectedPanelId 变化时 reset，别在每次 store 更新都 reset

updatedAt：每次 patch 更新 meta.updatedAt = new Date().toISOString()

I) 验收标准（必须全部满足）

选中某 Panel，右侧能看到完整 PanelSpec 并可修改 ✅

修改 shotType/weather/dialogue 等，Storyboard 卡片内容立即变化 ✅

刷新页面，所有修改仍存在（localStorage 草稿）✅

导出 JSON 后内容能再导入恢复 ✅

导入非法 JSON 会提示错误，不会破坏现有数据 ✅
```

------

## 任务包 04：Studio 的数据流（Panel 列表 + 选中 + Inspector 空壳）

```txt
✅ 任务包04：RenderJob 状态机 + Mock 渲染队列 + WS 实时更新
目标（必须达成）

Panel 状态机在前端跑通：Draft → Queued → Running → Rendered/NeedsFix ✅

Topbar 有按钮：渲染当前、渲染选中N个（至少当前）✅

底部 Jobs Console 显示队列：job 列表、状态、进度、错误 ✅

Storyboard 卡片状态 badge 实时更新 ✅

Mock WS 推送实时事件（progress/status/qa_result/layerpack_ready）✅

Rendered 后自动生成一个 mock LayerPack（full/bg/char/fg 的 url 可用占位）✅

NeedsFix 后进入左侧 NeedsFixList（占位也行，但要能点击定位到 panel）✅

“重试”按钮可用：Failed → Queued → 再跑 ✅

A) 新增类型与事件协议（前端先定死）

新增文件：

src/lib/schema/job.ts
src/lib/ws/events.ts
src/lib/ws/mockWsServer.ts
src/lib/ws/client.ts

1) RenderJob 类型（Zod + TS）

字段建议：

id, panelId, chapterId, projectId

provider: "comfyui" | "keling" | "tongyi" | "doubao" | "mock"

status: "Queued" | "Running" | "Succeeded" | "Failed"

progress: number (0..1)

createdAt, updatedAt

error?: string

qa?: { score: number; issues: string[] }

2) WS 事件协议（必须实现）

事件 types：

job_created

job_progress

job_status

panel_status

layerpack_ready

qa_result

统一格式：

type WsEvent = { type: string; payload: any; ts: string }

B) studioStore 扩展：jobs + layerpacks + needsFix 索引

修改 src/lib/store/studioStore.ts 增加：

必加字段

jobs: Record<string, RenderJob>

jobOrder: string[]（按创建时间排序）

layerPacks: Record<string, LayerPack>（mock 也要存）

panelLatestLayerPack: Record<string, string>（panelId -> layerPackId）

needsFixPanelIds: string[]

必加方法

enqueueRender(panelIds: string[], provider?: Provider)：创建 jobs（Queued）

applyWsEvent(event: WsEvent)：唯一入口，统一更新 jobs/panels/layerpacks

retryJob(jobId)：重建或复用 job（建议新 jobId）

getJobsForChapter(projectId, chapterId)：供 UI 显示

getSelectedJob()（可选）

状态更新规则（要写进 applyWsEvent）

job_created：jobs[jobId]=... + push order

job_progress：更新 progress，若 status 不是 Running 则设 Running

job_status Succeeded：

job.status=Succeeded

panel.render.status=Rendered

job_status Failed：

job.status=Failed + error

panel.render.status=NeedsFix

needsFixPanelIds push panelId（去重）

layerpack_ready：

layerPacks[layerPackId]=...

panelLatestLayerPack[panelId]=layerPackId

qa_result：

panel.qa=...

若 score 低于阈值（例如 <0.6），也可自动 NeedsFix（先不强制）

C) Mock 渲染引擎：用“假 WS 服务器”模拟真实推送

你现在没有真实后端推送也没关系：前端创建一个 mock server（纯前端 setTimeout）来模拟。

mockWsServer.ts 的行为

提供函数：

startMockJob(job: RenderJob, emit: (event: WsEvent)=>void)

逻辑：

emit job_created

0.2s 后 emit panel_status Queued

0.5s 后开始 Running，每 300ms progress +0.1（到 1.0）

随机失败率 15%（可配置）

失败：emit job_status Failed（带 error，如 "Character consistency low"）

成功：emit job_status Succeeded

成功后 emit layerpack_ready（生成一套占位图 URL）

成功后 emit qa_result（score 0.6~0.95，issues 随机 0~2 条）

占位图 URL：

你可以直接用 https://picsum.photos/seed/${panelId}/... 这种方式生成不同图片

full/bg/char/fg 都给不同 seed

D) WS Client：统一“订阅/反订阅/派发”

client.ts 暂时只需要 mock：

connect()：返回一个对象 { subscribeChapter, disconnect }

subscribeChapter(projectId, chapterId, onEvent)：内部使用 mock server 触发事件

后续替换成真实 WebSocket 时保持 API 不变

E) UI 改造：Topbar + Jobs Console + NeedsFixList
1) StudioTopbar 增加按钮（必须）

渲染当前（selectedPanelId）

渲染全部（chapter 所有 panels）【可选但强烈推荐】

Provider 选择（DropdownMenu）：Mock/ComfyUI/Keling/通义/豆包（先都走 mock）

点击后调用 store.enqueueRender([...panelIds], provider)

2) Bottom Jobs Console（必须）

你任务包02已经有底部 TimelineDock，占位即可；这里新增一个真正的 JobsConsole（或直接把 TimelineDock 改成双 Tab：Timeline / Jobs）

新增：

src/components/studio/bottom/JobsConsole.tsx


显示：

job 列表：时间、panel index、status、progress bar、provider

点击某 job：选中对应 panel

Failed 显示 error

Failed 有 “重试”按钮（retryJob）

3) Left NeedsFixList（必须）

在 src/components/studio/left/ScriptEditor.tsx 或单独组件里加一个区块：

列出 needsFixPanelIds（显示 panel index + error 摘要）

点击定位到该 panel（selectPanel + 更新 URL）

F) 将 Mock WS 真正接入 Studio 生命周期

在 StudioShell.tsx：

进入 studio：connect ws client

subscribe chapter：收到 event → store.applyWsEvent(event)

离开时 disconnect

并在 enqueueRender 后：

对每个新 job 调用 mockWsServer.startMockJob(job, emit)
（或由 ws client 统一处理）

G) 验收标准（必须全部满足）

进入 studio，选中 panel

点击 “渲染当前”

该 panel 状态变 Queued → Running（进度条动） → Rendered 或 NeedsFix

Jobs Console 能看到对应 job 和进度

Rendered 后，panel 记录 latestLayerPackId，中央预览（你任务包03的 Layers Tab）能看到 LayerPack 占位图

如果失败：NeedsFixList 出现该 panel，点击能定位

点击 “重试” 会创建新 job 并重新跑一遍

所有状态更新都来自统一的 applyWsEvent（不要在 UI 里直接改状态）
```

------

## 任务包 05：Konva 气泡编辑器（可用级）

```txt
✅ 任务包05：LayerPack Viewer + 局部重绘闭环
目标（必须达成）

Rendered 的 panel 在 CanvasStage 能看到“最终图(full)” ✅

Layers Tab 显示当前 panel 的 LayerPack（bg/char/fg/text/mask…） ✅

每个图层有：显示开关、透明度 slider（先做 0~1）✅

Center 预览区支持“合成预览”：按开关叠加 bg/char/fg ✅

支持在预览区画一个矩形选区（ROI），作为局部重绘区域 ✅

“局部重绘”弹窗：输入修复指令（prompt/negative）、选择策略（inpaint / redraw_char / redraw_bg / reroll）✅

点击执行后：创建 RenderJob（provider=mock/comfyui）并走状态机，完成后生成新的 LayerPack 版本 ✅

LayerPack 版本管理：同一个 panel 可有多个 layerpack，能切换版本 ✅

NeedsFix 面板支持“一键修复”入口（打开局部重绘弹窗）✅

A) 新增 LayerPack 数据模型（最小但可扩展）

新增文件：

src/lib/schema/layerPack.ts
src/lib/schema/fixPlan.ts

1) LayerPack schema（Zod）

字段建议：

id: string

panelId: string

version: number

createdAt: string

engine: { provider: Provider; modelHint?: string }

outputs:

full: { url: string; w: number; h: number }

bg?, char?, fg?, text?: 同结构

mask?: { url: string; kind: "char"|"roi"|"depth"|"seg"; w; h }

meta:

seed?, steps?, cfg?

notes?: string

qa?: { score: number; issues: string[] }

2) FixPlan schema（Zod）

字段：

panelId

layerPackId（当前版本）

strategy: "inpaint"|"redraw_char"|"redraw_bg"|"reroll"

roi: { x: number; y: number; w: number; h: number } | null

prompt: string

negative: string

strength: number (0..1)

seedMode: "keep"|"new"

createdAt

B) store 扩展：layerpack 版本树 + viewer 状态

修改 src/lib/store/studioStore.ts 增加：

必加字段

layerPacksByPanel: Record<string, string[]>
panelId -> [layerPackId1, layerPackId2,...] 按 version 排序

selectedLayerPackIdByPanel: Record<string, string>
panelId -> 当前选中的 layerPackId

viewer:

layerVisibility: { bg: boolean; char: boolean; fg: boolean; text: boolean }

layerOpacity: { bg: number; char: number; fg: number; text: number }

roi: { x:number;y:number;w:number;h:number } | null

isRoiSelecting: boolean

fixModal:

open: boolean

draft: FixPlan | null

必加方法

selectLayerPack(panelId, layerPackId)

setLayerVisibility(layer, bool)

setLayerOpacity(layer, value)

setRoi(roi|null)

openFixModal(panelId, strategy?)

closeFixModal()

submitFixPlan(fixPlan)：创建 RenderJob 并启动 mock job（或走 ws client）

applyWsEvent 扩展（与任务包04对齐）

当收到 layerpack_ready：

更新 layerPacks（已有）

同时写入 layerPacksByPanel[panelId] 追加

自动把当前 panel 的 selectedLayerPack 指向最新版本

注意：同一个 panel 可能多次渲染，layerpack version 要自动递增。

C) Mock WS 扩展：支持 FixPlan 驱动的“局部重绘”事件

修改：

src/lib/ws/mockWsServer.ts

新增：

startMockFixJob(job, fixPlan, emit)

行为：

失败率稍低（例如 10%）

成功后 layerpack_ready 的 outputs 根据 strategy 不同：

inpaint：full 改 seed；bg/char/fg 随机改其中一个（模拟局部修复）

redraw_char：char url 改、full url 改

redraw_bg：bg url 改、full url 改

reroll：全部 url 改

qa_result：issues 更少（模拟修复成功）

D) UI：CanvasStage 升级为 LayerPack Viewer（核心）

修改：
src/components/studio/center/CanvasStage.tsx

要求（可用方案：纯 HTML + 绝对定位叠图）

如果当前 panel 有 LayerPack：

用 <div className="relative">

<img> 叠加显示 bg/char/fg/text

每层按 opacity 与 visibility 控制：style={{opacity}} + hidden

如果没有 LayerPack：

显示占位提示 “尚未渲染”

ROI 选择：

鼠标按下记录起点

鼠标移动绘制矩形 overlay（半透明边框）

鼠标抬起固定 roi 写入 store.viewer.roi

提供“清除选区”按钮

显示当前选区数值（x,y,w,h），方便后端对齐

ROI 不需要像素级精准到图像坐标转换，第一版可以按容器坐标保存；后续接真实渲染时再加入换算（基于原图 w/h 与容器缩放比）。

E) UI：Inspector Layers Tab 变成真正 LayerPack 控制面板

修改：
src/components/studio/right/InspectorLayers.tsx

必须包含

当前 layerpack 版本选择（DropdownMenu 或 Select）

显示 v1/v2/v3 + createdAt

图层开关（bg/char/fg/text）+ opacity slider

显示每个图层缩略（可选，用小 img）

显示 QA score 与 issues

按钮：

“局部重绘（inpaint）”

“重绘角色”

“重绘背景”

“重新抽卡（reroll）”
点击后 openFixModal(strategy)

F) UI：Fix Modal（修复弹窗）

新增组件：

src/components/studio/modals/FixModal.tsx

弹窗内容

strategy 选择（Tabs/Select）

prompt（Textarea）

negative（Textarea）

strength slider（0.1~1.0）

seedMode（keep/new）

ROI 信息显示（如果 roi=null 提示“未选择选区，将对整图生效”）

执行按钮：执行修复

取消按钮

执行后：

store.submitFixPlan(fixPlan)

关闭弹窗（或保持打开显示 job 进行中也行）

G) 连接点：NeedsFixList 加“一键修复”

在 NeedsFixList 的每项旁边加：

“修复”按钮 → openFixModal(panelId, "inpaint")

H) 验收标准（必须全部满足）

Rendered 的 panel 在中间预览能看到图片（叠图） ✅

Layers Tab 能切换 v1/v2 并更新预览 ✅

勾选/取消某图层，预览实时变化 ✅

拖 opacity，预览透明度变化 ✅

在预览区拖拽画 ROI，能看到选区框，并保存到 store ✅

打开 FixModal 填 prompt，执行后生成新 job，成功后生成新的 layerpack v+1 ✅

修复成功后，selectedLayerPack 自动切到最新版本 ✅

全过程由 WS 事件驱动（applyWsEvent 统一更新） ✅
```

------

## 任务包 06：LayerPack Viewer（Flux 分层预览与层开关）

```txt
✅ 任务包06：Timeline + Clip/VideoJob + ExportSpec
目标（必须达成）

底部从占位变成真正的 Timeline（Clip 列表 + 总时长 + 顺序可调）✅

把 Panel 变成 Clip：Add to Timeline ✅

每个 Clip 可编辑：时长、FPS、Provider、motion prompt、seedMode ✅

每个 Clip 可 “生成运动视频”（先 mock）并走状态机：Idle → Queued → Running → Succeeded/Failed ✅

Clip 产物：clip.output.videoUrl（mock）✅

章节导出：生成 export_spec.json（含 clip 顺序、时长、provider、输入帧、输出视频 url）✅

导出也走 job：export_job 进度条 + ready 事件 ✅

全部状态更新仍然统一走 store.applyWsEvent(event) ✅

A) 新增 Schema：Timeline / Clip / VideoJob / ExportSpec

新增文件：

src/lib/schema/timeline.ts
src/lib/schema/clip.ts
src/lib/schema/videoJob.ts
src/lib/schema/exportSpec.ts
src/lib/schema/provider.ts

1) Provider 统一枚举（前端协议先定死）

Provider：

"mock"

"comfyui_svd"（图生视频/帧序列类）

"kling"

"tongyi"

"doubao"

2) Clip（核心）

字段建议（第一版够用但可扩展）：

id: string

projectId, chapterId

panelId: string（来源分镜）

layerPackId: string | null（用哪个版本的画面做输入；默认取 selectedLayerPack）

type: "motion_clip"（后续可扩 "transition" "audio"）

durationSec: number（0.5~10；后续可放宽）

fps: number（6/8/12/24）

provider: Provider

motionPrompt: string（运镜+动作）

negative: string

seedMode: "keep" | "new"

status: "Idle" | "Queued" | "Running" | "Succeeded" | "Failed"

progress: number

output?: { videoUrl: string; previewUrl?: string; frames?: string[] }

error?: string

createdAt, updatedAt

注意：你之前问“为什么图生视频生成的是图片？”——很多 ComfyUI 的视频 workflow（比如 SVD）输出本质是 帧序列 或 latent → decode 的多帧，你需要后处理合成 mp4。这里 Clip.output 允许 frames[] 和 videoUrl 同时存在，为未来预留。

3) VideoJob（针对某个 clip 的生成任务）

id, clipId, provider

status: "Queued"|"Running"|"Succeeded"|"Failed"

progress: number

createdAt, updatedAt

error?: string

4) Timeline（按章节维度）

projectId, chapterId

clips: Clip[]

settings: { fpsDefault: number; aspect: "9:16"|"16:9"|"1:1"; exportPreset: "draft"|"final" }

meta: { updatedAt }

5) ExportSpec（商业化最重要的“交付合同”）

projectId, chapterId, version

timeline: { fps, aspect, totalDurationSec }

clips: Array<{ order: number clipId: string panelId: string layerPackId: string|null provider: Provider durationSec: number fps: number motionPrompt: string input: { imageUrl: string; endImageUrl?: string } // 未来支持首尾帧 output?: { videoUrl?: string; frames?: string[] } }>

generatedAt

notes?: string

B) store 扩展：timeline/clips + videoJobs + exportJobs

修改：src/lib/store/studioStore.ts

必加字段

timelineByChapter: Record<string, Timeline>
key = ${projectId}:${chapterId}

videoJobs: Record<string, VideoJob>

videoJobOrder: string[]

exportJobs: Record<string, { id; status; progress; outputSpecUrl?; error?; createdAt; updatedAt }>

exportJobOrder: string[]

selectedClipId: string | null

必加方法

getTimelineKey(projectId, chapterId)

ensureTimeline(projectId, chapterId)

addPanelAsClip(panelId, options?)

默认：layerPackId = 当前 panel 选中的 layerpack（如果没有则 null）

provider 默认 = "mock"

fps 默认 = 6 或 timeline.settings.fpsDefault

duration 默认取 PanelSpec.shot.durationSec（你任务包03已有）

removeClip(clipId)

reorderClip(clipId, toIndex)（先做上移/下移，后续再做 DnD）

updateClip(clipId, patch)

selectClip(clipId)

enqueueClipRender(clipId)：创建 videoJob & 发起 mock

retryClipRender(clipId)

buildExportSpec(projectId, chapterId)：组装 ExportSpec（从 timeline+layerpack+panelSpec）

enqueueExport(projectId, chapterId)：创建 exportJob 并发起 mock

applyWsEvent(event)：扩展支持视频/导出事件（见下）

C) WS 事件扩展（仍然统一 applyWsEvent）

扩展 src/lib/ws/events.ts 类型（保持同一 WsEvent 协议）：

新增事件 types：

clip_added

clip_updated

video_job_created

video_job_progress

video_job_status

clip_status

clip_output_ready

export_job_created

export_job_progress

export_job_status

export_ready（返回 exportSpec 的下载 url 或 JSON 内容）

规则：UI 不直接改 clip.status/job.progress，全部由 applyWsEvent 驱动。

D) Mock 渲染扩展：startMockVideoJob + startMockExportJob

修改：src/lib/ws/mockWsServer.ts

新增函数：

startMockVideoJob(videoJob, clip, emit)

startMockExportJob(exportJob, exportSpec, emit)

MockVideoJob 行为（建议）

失败率：10~15%

Running 期间：每 250ms progress + 0.08

成功：

emit video_job_status(Succeeded)

emit clip_status(Succeeded)

emit clip_output_ready：

videoUrl 用 placeholder：https://samplelib.com/lib/preview/mp4/sample-5s.mp4 或你自己静态资源（如果网络不可控就改成本地占位）

同时生成 frames[]：https://picsum.photos/seed/${clipId}-${i}/512/896（用于模拟“帧序列输出”）

失败：

emit video_job_status(Failed, error)

emit clip_status(Failed, error)

MockExportJob 行为

progress 0→1

成功后 emit export_ready：提供 exportSpec JSON（直接放 payload 里也可以）

同时 emit export_job_status(Succeeded)

E) UI 改造：TimelineDock → TimelinePanel（真正可用）

你任务包02/04 里底部已经有 Dock 占位，这一包把它升级成带 Tabs：

新增/修改：

src/components/studio/bottom/TimelineDock.tsx   // 改为 Tabs：Timeline / Jobs
src/components/studio/bottom/TimelinePanel.tsx // 新增
src/components/studio/bottom/ClipRow.tsx       // 新增

1) TimelinePanel 必须包含

顶部汇总条：

总 Clip 数

总时长（sum durationSec）

默认 FPS / Aspect（可编辑）

导出按钮（enqueueExport）

Clip 列表（纵向）：

左侧：序号 + panel index

中间：clip 参数（duration/fps/provider）可编辑

右侧：状态 badge + progress bar + “生成/重试”按钮

上移/下移按钮（reorderClip）

删除按钮

空状态提示：

“从 Storyboard 选择一个分镜，点击 Add to Timeline”

2) ClipRow 可编辑字段（第一版必须）

durationSec：Input（number）

fps：Select（6/8/12/24）

provider：Select（mock/comfyui_svd/kling/tongyi/doubao）

motionPrompt：Input 或 Textarea（短）

status + progress

按钮：

Generate（Idle/Failed 可点）

Retry（Failed 可点）

Focus（定位到对应 panel）

Use Latest LayerPack（一键把 layerPackId 设为最新版本）

F) Storyboard 与 Timeline 的连接：一键加入 Clip

修改这些组件（任选其一加入口）：

src/components/studio/center/PanelCard.tsx：加按钮 “+ Timeline”
或

InspectorStory.tsx：加按钮 “Add to Timeline”

逻辑：

选中 panel 后点击 → addPanelAsClip(panelId)

G) 右侧 Inspector 新增 “Clip Inspector”（可选但推荐）

为了商业化可控，你最终会编辑“每个片段的运镜/动作/节奏”。这包先做最小：

新增：

src/components/studio/right/InspectorTimeline.tsx


在 InspectorTabs 增加一个 Tab：Timeline

如果选中 clip：显示 motionPrompt / provider / duration / fps

如果没选中：提示 “请在 Timeline 选中一个 clip”

这能让你的编辑路径更像 CapCut：底部选片段，右边调参数。

H) 导出：ExportSpec 下载（商业化交付关键）

修改 StudioTopbar.tsx 或 TimelinePanel 顶部加：

“导出 ExportSpec”按钮：

如果有 export_ready payload：直接下载 json

或点击先 enqueueExport（走进度）→ 完成后下载

实现下载：

const blob = new Blob([JSON.stringify(spec,null,2)], {type:'application/json'})

a.href = URL.createObjectURL(blob)

I) 验收标准（必须全部满足）

选中某 panel → 点击 “+ Timeline” → Timeline 出现一个 clip ✅

可编辑时长/FPS/provider/motionPrompt，修改后不丢（仍走草稿保存：localStorage）✅

点击 “Generate” → Clip 状态 Queued→Running（进度条动）→ Succeeded/Failed ✅

成功后 clip.output 有 videoUrl 或 frames[] ✅

点击 “导出” → 得到 export_spec.json，包含 clip 顺序、时长、输入图（layerpack.full.url）、输出视频/帧 ✅

失败 clip 可 Retry ✅

任意状态变化都从 applyWsEvent 进来（UI 不直接改 status）✅

yarn dev 无 TS 报错 ✅

J) 本地草稿持久化：Timeline 也要保存

把 Timeline 也纳入 chapterDraft（你任务包03已经保存 PanelSpec）：

chapterDraft 结构扩展为：

panelSpecs

timeline（clips + settings）

layerpacks（可选：你也可以只存引用，mock 期先全存）

localStorage key 仍然：
ai-webtoon:chapterDraft:${projectId}:${chapterId}
```

------

## 任务包 07：Jobs Console + WebSocket 实时更新

```txt
✅ 任务包07：双关键帧 Clip（Start/End）+ 多轨道基础 + Provider 协议
目标（必须达成）

每个 motion clip 支持 startFrame 与 endFrame 两个关键帧输入 ✅

关键帧可来自：

选中的 Panel 的某个 LayerPack.full

Timeline 中指定其它 Panel 的 LayerPack.full

上传图片（先做占位：Asset 引用即可）✅

Clip Inspector 支持选择 start/end ✅

Clip 生成时把 start/end 写入 job payload（mock 也要体现）✅

Mock 视频输出：根据 start/end seed 生成不同 videoUrl/frames（体现“首尾帧驱动”）✅

Timeline 支持多轨道雏形：Video Track + Text Track（占位）✅

转场（Transition）占位：在两个视频 clip 之间插入 transition item（只影响 export spec）✅

ExportSpec 升级：包含 startFrame/endFrame 与 transition 列表 ✅

A) 升级 Clip Schema：加入关键帧输入与轨道

修改/新增文件：

src/lib/schema/clip.ts         (升级)
src/lib/schema/timeline.ts     (升级)
src/lib/schema/track.ts        (新增)
src/lib/schema/keyframe.ts     (新增)
src/lib/schema/transition.ts   (新增)
src/lib/schema/exportSpec.ts   (升级)

1) KeyframeRef（关键帧引用）

新增 keyframe.ts：

type KeyframeRef =
  | { type: "layerpack"; panelId: string; layerPackId: string; url: string; w: number; h: number }
  | { type: "asset"; assetId: string; url: string; w: number; h: number }


注意：为了让生成 job 不依赖 store 查询，KeyframeRef 里直接带 url/w/h。

2) Clip 升级字段（必加）

在 Clip 中加入：

startFrame: KeyframeRef | null

endFrame: KeyframeRef | null

motionMode: "single_keyframe" | "dual_keyframe"

默认 dual_keyframe（你目标）

cameraPlan?: { move: string; intensity: number; beat: "slow"|"medium"|"fast" }（可选）

并把 type 扩到：

"motion_clip"

"transition"（也可以单独 schema，第一版放一起也行）

3) Track（多轨道）

新增 track.ts：

VideoTrack：items = Clip[]

TextTrack：items = SubtitleItem[]（先占位）

Timeline 结构升级为：

tracks: { video: Clip[]; text: SubtitleItem[] }

transitions: Transition[]（或 transition 作为 video track item 也行，但导出更麻烦）

第一版不要做复杂 DnD，多轨道就是数据结构+UI占位即可。

4) Transition（转场占位）

新增 transition.ts：

id

between: { fromClipId: string; toClipId: string }

type: "cut"|"fade"|"dissolve"|"push"

durationSec: number（0~1.5）

easing?: string

B) store 升级：Timeline 从 clips[] -> tracks.video[] + transitions[]

修改 studioStore.ts：

必加字段

timelineByChapter[key].tracks.video: Clip[]

timelineByChapter[key].transitions: Transition[]

selectedTimelineItem: { kind:"clip"|"transition"|"subtitle"; id:string } | null

必加方法

addPanelAsClip(panelId, opts?)

默认 startFrame = 当前 panel 的 selected layerpack full

默认 endFrame = 同一个 startFrame（如果没有 end 的来源，先等用户指定）

motionMode 默认 dual_keyframe，但 endFrame null 时生成按钮禁用或提示

setClipStartFrame(clipId, keyframeRef|null)

setClipEndFrame(clipId, keyframeRef|null)

swapClipFrames(clipId)（方便调试）

addTransition(fromClipId, toClipId, type?, duration?)

removeTransition(id)

updateTransition(id, patch)

buildExportSpec() 升级输出关键帧与 transitions

C) UI：TimelinePanel 升级显示 Start/End 状态与转场

修改：
TimelinePanel.tsx, ClipRow.tsx

ClipRow 必须新增展示

StartFrame 状态：

有：显示 “S ✅”

无：显示 “S ⚠”

EndFrame 状态：

有：显示 “E ✅”

无：显示 “E ⚠”

Generate 按钮规则：

dual_keyframe 模式：start && end 都有才可点

single_keyframe：只要 start 有即可点

转场 UI（第一版最小）

在两个 clip 之间显示一条 “Transition bar”：

若存在 transition：显示 type + duration + Edit 按钮

若不存在：显示 “+ Add Transition”（默认 cut 或 fade 0.3s）

你可以先不做可视化插入条，先在 ClipRow 右侧提供：

“Add Transition to Next” 按钮（只对非最后一个 clip）

D) UI：Clip Inspector（关键帧选择器）

新增/修改：
src/components/studio/right/InspectorTimeline.tsx

必须包含

motionMode 选择（single/dual）

StartFrame Picker

EndFrame Picker

快捷按钮：

“Use current panel as Start”

“Use current panel as End”

“Use latest layerpack”

输入框：

motionPrompt

durationSec

provider

关键帧选择器怎么做（第一版可用方案）
方案 1（最快）：从“章节面板列表”选择

下拉框列出所有 panel：

Panel 01 - 旧书店门口（用 PanelSpec.scene.location 派生）

选中后自动取该 panel 当前 selectedLayerPack 的 full 作为 keyframeRef

方案 2（更强）：从该 panel 的 layerpack 历史版本选

选 panel 后，再选 v1/v2/v3

取对应 layerPack.outputs.full.url

第一版至少做方案1，方案2可选。

E) MockVideoJob 升级：体现“首尾帧驱动”

修改 startMockVideoJob：

当 clip.motionMode === dual_keyframe：

必须读取 clip.startFrame.url 与 clip.endFrame.url

输出 videoUrl/frames seed 里包含 start/end hash，确保不同关键帧产生不同产物

例如：

frames: picsum.photos/seed/${clipId}-S-${i}/... 前半段

picsum.photos/seed/${clipId}-E-${i}/... 后半段（模拟向 endFrame 过渡）

并 emit 一个新事件：

clip_keyframes_used（可选）：用于调试显示

F) ExportSpec 升级（商业化契约）

修改 exportSpec.ts：
每个 clip 输出必须包含：

startFrame?: { url,w,h,source }

endFrame?: { url,w,h,source }

motionMode

transitionToNext?: { type, durationSec }（你可以在导出时把 transitions 合并到 clip 上，方便下游消费）

导出建议结构：

{
  "timeline": { "fps": 24, "aspect": "9:16", "totalDurationSec": 61.2 },
  "videoTrack": [
    {
      "order": 0,
      "clipId": "...",
      "panelId": "...",
      "provider": "kling",
      "durationSec": 7,
      "motionMode": "dual_keyframe",
      "startFrame": {...},
      "endFrame": {...},
      "motionPrompt": "...",
      "transitionToNext": {"type":"fade","durationSec":0.3},
      "output": {"videoUrl":"..."}
    }
  ],
  "textTrack": [],
  "generatedAt": "..."
}

G) Provider 适配层：统一 payload（未来接真实模型）

新增文件：

src/lib/providers/videoProvider.ts
src/lib/providers/payloadBuilders.ts


第一版只需要：

buildVideoGenPayload(clip: Clip)：输出统一 payload

payload 字段必须包含：

startFrame.url / endFrame.url

durationSec, fps, motionPrompt, negative

provider

seedMode

Mock server 也用这个 payload（保证未来替换不改 UI）。

H) 验收标准（必须全部满足）

Timeline 加入 2 个 clips ✅

在 Clip Inspector 设置 start/end（来自不同 panel）✅

dual_keyframe 模式下 endFrame 没有时 Generate 按钮禁用并提示 ✅

设置完 start/end，点击 Generate，状态跑通 ✅

ExportSpec 导出包含 startFrame/endFrame + transitions ✅

mock output frames/videoUrl 因关键帧不同而不同（肉眼可见不同 seed）✅

刷新页面，timeline/关键帧选择/转场仍存在（草稿持久化）✅
```

------

## 任务包 08：导出页（长条漫 PNG / 微动 MP4 的入口）

```txt
✅ 任务包08：Consistency + Anchor + QA Auto Retry + Provider Fallback
目标（必须达成）

建立“资产绑定系统”：Panel/Clip 可绑定 角色资产（LoRA/Embedding/FaceID） 与 场景资产（背景库/场景LoRA/锚点） ✅

Scene Anchor：每个 Panel 支持一个或多个 AnchorSpec（控制图策略：depth/pose/seg/lineart…），并能在 UI 里选择/生成（先 mock）✅

视频 Clip 的生成 payload 必须携带：characters[]、sceneAnchor[]、styleProfile ✅

QA 系统：每次生成后有 qa.score 与 qa.issues ✅

Auto Retry：当 score < 阈值，自动触发重试（最多 N 次、总预算 M token/cost）✅

Fallback：重试时可自动切换 provider（如 Kling → 通义 → 豆包 → ComfyUI）✅

失败/质量差时进入 NeedsFix：可“一键修复（inpaint/redraw_char）”并自动挂载最可能的修复策略 ✅

所有状态更新统一走 applyWsEvent（仍然不允许 UI 直接改状态）✅

A) 新增核心 Schema：IdentityAsset / SceneAsset / AnchorSpec / QAConfig / RetryPolicy / GenerationContext

新增文件：

src/lib/schema/assets.identity.ts
src/lib/schema/assets.scene.ts
src/lib/schema/anchor.ts
src/lib/schema/qa.ts
src/lib/schema/retryPolicy.ts
src/lib/schema/generationContext.ts
src/lib/schema/styleProfile.ts

1) IdentityAsset（角色一致性资产）

最小字段：

id

type: "lora" | "embedding" | "faceid" | "refnet"

name

tags: string[]（如 “主角/周屿/青年/短发”）

providerHint?: Provider

files:

{ kind:"safetensors"|"embedding"|"image_ref"|"json"; url:string }[]

params:

strength?: number

triggerWords?: string[]（LoRA触发词）

createdAt

2) SceneAsset（场景/背景资产）

id

type: "bg_pack" | "scene_lora" | "style_lora" | "palette" | "lighting_preset"

name

files: {kind; url}[]

params?: { strength?: number; promptAddon?: string }

3) AnchorSpec（场景锚点/控制图）

id

panelId

kind: "depth"|"pose"|"seg"|"lineart"|"canny"|"scribble"

source:

{ type:"image"; url }（从关键帧/分层图生成）

controlImage: { url; w; h } | null （mock生成）

weight: number (0..2)

notes?: string

4) StyleProfile（风格档案，商业化稳定很重要）

id, name

baseModelHint（如 flux / sd / 具体模型名）

promptPrefix, negativePrefix

colorTone, lineStyle, renderStyle

refImages?: string[]

defaultCfg/steps/denoise

5) QAConfig（质量门槛）

threshold: number（例如 0.75）

maxIssues: number（例如 3）

rulesEnabled: { identity: boolean; anatomy: boolean; text: boolean; flicker: boolean }

6) RetryPolicy（重试策略）

maxAttempts: number（例如 3）

providerChain: Provider[]（如 ["kling","tongyi","doubao","comfyui_svd","mock"]）

budget: { maxCost: number; costUsed: number }

backoffMs: number（mock里可不用）

onLowScore: "retry" | "needs_fix"

autoFixStrategy: "inpaint"|"redraw_char"|"reroll"

7) GenerationContext（把一切绑定到一次生成上）

clipId? / panelId?

styleProfileId

identityAssetIds: string[]

sceneAssetIds: string[]

anchorIds: string[]

qaConfig

retryPolicy

GenerationContext 是将来你对接后端和数据库最关键的一层：它让“同一条生产线”能复现。

B) store 扩展：资产库 + 绑定关系 + 默认配置

修改 src/lib/store/studioStore.ts：

必加字段

identityAssets: Record<string, IdentityAsset>

sceneAssets: Record<string, SceneAsset>

anchors: Record<string, AnchorSpec>

styleProfiles: Record<string, StyleProfile>

绑定索引（核心）

panelBindings: Record<string, { identityAssetIds: string[]; sceneAssetIds: string[]; anchorIds: string[]; styleProfileId: string|null }>

clipBindings: Record<string, { identityAssetIds: string[]; sceneAssetIds: string[]; anchorIds: string[]; styleProfileId: string|null; qaConfig?: QAConfig; retryPolicy?: RetryPolicy }>

默认配置

defaults: { styleProfileId: string|null; qaConfig: QAConfig; retryPolicy: RetryPolicy }

必加方法

bindIdentityToPanel(panelId, assetId)

unbindIdentityFromPanel(panelId, assetId)

bindSceneToPanel(panelId, assetId)

bindAnchorToPanel(panelId, anchorId)

setPanelStyleProfile(panelId, styleProfileId)

applyPanelBindingsToClip(clipId)（一键把 panel 的绑定复制到 clip）

setClipBindings(clipId, patch)

setClipQAConfig(clipId, qaConfig)

setClipRetryPolicy(clipId, retryPolicy)

createMockAnchor(panelId, kind)：生成 controlImage（picsum占位）

buildGenerationContextForClip(clipId)：合并默认+clip绑定+panel绑定（优先级：clip > panel > defaults）

C) Provider Payload Builder 升级：必须携带一致性/锚点/风格/QA/重试信息

修改 src/lib/providers/payloadBuilders.ts：

新增：

buildVideoGenPayload(clip, ctx)（ctx=GenerationContext）
payload 里必须包含：

startFrame/endFrame

motionPrompt/negative

styleProfile（prefix/negativePrefix/params）

identityAssets（至少给出 ids + triggerWords/strength + file urls）

sceneAssets（ids + promptAddon + file urls）

anchors（kind + controlImage.url + weight）

qaConfig

retryPolicy（用于后端也可参考，但前端也要用）

D) WS 事件扩展：QA + AutoRetry + Fallback 透明化

修改 src/lib/ws/events.ts：

新增事件：

qa_result（已存在，可扩展字段）

retry_scheduled（attempt n, nextProvider）

provider_fallback（from->to）

budget_updated（costUsed）

needs_fix_suggested（strategy + reason）

anchor_generated（anchorId + controlImage）

E) Mock 引擎升级：QA 评分 + 自动重试 + provider fallback（重点）

修改 src/lib/ws/mockWsServer.ts：

1) 模拟 cost 与 score

每次 video job 成本 cost：按 provider 给不同权重

kling 5

tongyi 4

doubao 3

comfyui_svd 2

mock 1

QA score 随机，但受“资产绑定程度”影响（模拟商业化逻辑）：

若 identityAssets >=1：score +0.08

若 anchors >=1：score +0.06

若 styleProfile 有：score +0.05

若 dual_keyframe：score +0.03

issues 也随之减少

2) 自动重试逻辑（必须）

当 video job Succeeded 后：

emit qa_result(score, issues)

若 score < threshold：

读取 ctx.retryPolicy

若 attempts < maxAttempts 且 budget 未超：

emit retry_scheduled(attempt+1, nextProvider)

emit provider_fallback(from,to)（如果 provider 变了）

创建一个新 video_job（或复用同 clip 但 new jobId）

clip.status 重新变 Queued→Running

否则：

emit needs_fix_suggested(strategy, reason)

emit clip_status(Failed or NeedsFix)（你可以新增 clip_status="NeedsFix"，或沿用 Failed + reason）

同时把对应 panel 推到 needsFixPanelIds（如果 clip 有 panelId）

关键：重试次数、预算、provider chain 都必须可配置（来自 ctx.retryPolicy）。

3) 自动修复建议

当触发 needs_fix_suggested：

若 issues 包含 “identity” → 建议 redraw_char

若 issues 包含 “background mismatch” → redraw_bg

若 issues 包含 “artifact” → inpaint

否则 reroll

F) UI：新增 “Consistency & QA” 面板（右侧 Inspector 扩展）

新增组件：

src/components/studio/right/InspectorConsistency.tsx
src/components/studio/right/InspectorQA.tsx
src/components/studio/right/InspectorAnchors.tsx


在 InspectorTabs 增加三个 Tab：

Consistency（角色/场景资产绑定）

Anchors（控制图）

QA/Retry（阈值/重试/预算/provider链）

1) Consistency Tab（最小可用）

IdentityAssets 列表（mock 先内置 2-3 个）：

周屿（LoRA） strength slider

林知夏（Embedding）

SceneAssets 列表：

旧书店门口（bg_pack）

雨天光照（lighting preset）

当前选中对象：若选中 panel，则编辑 panelBindings；若选中 clip，则编辑 clipBindings

“Apply panel bindings to clip” 按钮（把 panel 绑定一键复制到 clip）

2) Anchors Tab（最小可用）

当前 panel anchors 列表

“Generate Anchor”按钮：

kind 下拉：depth/pose/seg/lineart

点击后调用 createMockAnchor(panelId, kind)

Anchor 卡片：显示 kind、weight slider、controlImage 缩略图

3) QA/Retry Tab（最小可用）

QAConfig：

threshold slider（0.5~0.95）

RetryPolicy：

maxAttempts（1~5）

providerChain 多选/排序（第一版可用固定排序按钮：上移/下移）

budget.maxCost、costUsed 显示

onLowScore (retry/needs_fix)

autoFixStrategy（默认 inpaint）

当前 clip 最近一次 QA score/issue 展示

G) Timeline/ClipRow 显示“尝试次数/当前 provider/预算”

修改 ClipRow.tsx：

显示：

attempt: n / maxAttempts

provider：当前 provider（如果 fallback 了要变化）

budget：costUsed / maxCost（进度条）

当 needs_fix_suggested 后：

ClipRow 显示 “NeedsFix (suggest inpaint)”

点击 “Fix” 直接打开你任务包05的 FixModal，并自动填 strategy+prompt 建议

H) ExportSpec 升级：记录一致性/锚点/QA 轨迹（商业化追责）

修改 buildExportSpec：
每个 clip 增加：

generationContextSummary：

styleProfileId

identityAssetIds

sceneAssetIds

anchorKinds

qaThreshold

retry: { maxAttempts, providerChain, budget }

qaHistory: [{attempt, provider, score, issues, cost}]（mock 可生成）

这让你对外“交付可追溯”，商业化非常关键。

I) 验收标准（必须全部满足）

有 identityAssets/sceneAssets/styleProfiles 的 mock 数据，UI 可绑定到 panel/clip ✅

Anchors Tab 可生成一个 depth/pose anchor，并显示 controlImage ✅

Clip Generate payload 里包含 identityAssets + anchors + styleProfile ✅

mock 生成后会出 qa_result（score+issues）✅

score < threshold 会自动 retry，最多 N 次，并可能 fallback provider ✅

budget 用尽或达到 maxAttempts 会进入 NeedsFix，并给出 suggested strategy ✅

点击 Fix 会打开 FixModal 且默认策略已填 ✅

ExportSpec 包含 generationContextSummary + qaHistory ✅
```

------

## 任务包09：工业化连载与运营能力（V2 的骨架）

### 这一包的目标（必须达成）

1. **模板化**：一键套用「风格包 + 角色包 + 场景包 + QA/重试策略」到章节/分镜/时间轴 ✅
2. **版本树/协作雏形**：Chapter Draft 支持 commit / branch / compare（前端先做，后端接口预留）✅
3. **批量队列**：支持“本章全部 Clips 批量生成/暂停/继续/取消”✅
4. **成本与质量仪表盘**：按章/按 provider/按角色资产统计：成本、成功率、重试率、平均 QA score ✅
5. **发布与交付包**：生成 `release_bundle.json`（包括 export_spec + manifest 索引 + QA 报告 + 成本报表）✅

> 做完任务包09，你前端就像一个“漫剧工厂控制台”，能真正跑连载、跑批处理、做运营复盘。

------

# A) 新增 Schema：Template / VersionGraph / BatchQueue / Analytics / ReleaseBundle

新增文件：

```
src/lib/schema/template.ts
src/lib/schema/versionGraph.ts
src/lib/schema/batchQueue.ts
src/lib/schema/analytics.ts
src/lib/schema/releaseBundle.ts
```

## 1) Template（生产模板）

- `id, name, description`
- `styleProfileId`
- `identityAssetIds[]`
- `sceneAssetIds[]`
- `anchorDefaults`: { kinds: AnchorKind[]; weights: Record<kind,number> }
- `qaConfig`
- `retryPolicy`
- `motionDefaults`: { fpsDefault; durationDefault; providerChainDefault; promptPreset? }
- `createdAt`

## 2) VersionGraph（版本树）

- `commits: Record<commitId, Commit>`
- `branches: Record<branchName, commitId>`
- `head: { branch: string; commitId: string }`
- `Commit`：
  - `id, parentId?, message, author, createdAt`
  - `snapshotRef`: 指向一个 chapterDraft 的序列化快照（存在 localStorage / 或后端）

> 第一版：**全部前端 localStorage** 做版本树即可；后端后续接入。

## 3) BatchQueue（批量任务队列）

- `id, chapterId`
- `items: Array<{ clipId, status, progress, attempt, provider }>`
- `status: "Idle"|"Running"|"Paused"|"Completed"|"Canceled"`
- `controls`: { concurrency: number; stopOnNeedsFix: boolean }
- `stats`: { done, failed, needsFix, running }

## 4) Analytics（统计）

- `byProvider`: costUsed, avgScore, successRate, retryRate
- `byCharacter`: assetId -> same fields
- `chapterSummary`: totalCost, avgScore, totalClips, succeeded, needsFix, failed

## 5) ReleaseBundle（发布交付包）

- `projectId, chapterId, releaseVersion`
- `exportSpec`（任务包06/07）
- `layerPacksIndex`: { panelId -> layerPackId -> manifestUrl }
- `qaReport`: { clipId -> qaHistory }
- `analytics`
- `generatedAt`
- `notes`

------

# B) store 扩展：模板、版本、批量队列、分析

修改：`src/lib/store/studioStore.ts`

## 必加字段

- `templates: Record<string, Template>`
- `versionGraphsByChapter: Record<key, VersionGraph>`
- `batchQueuesByChapter: Record<key, BatchQueue>`
- `analyticsByChapter: Record<key, Analytics>`
- `releaseBundlesByChapter: Record<key, ReleaseBundle>`
- `ui`: { showTemplateModal; showVersionModal; showBatchModal; showAnalyticsPanel }

## 必加方法（模板）

- `applyTemplateToChapter(chapterId, templateId, scope)`
  - scope: `"chapter_defaults"|"all_panels"|"all_clips"|"selected_only"`
- `applyTemplateToPanels(panelIds[], templateId)`
- `applyTemplateToClips(clipIds[], templateId)`
- `saveCurrentAsTemplate(name, description)`（从 defaults+当前绑定生成）

## 必加方法（版本树）

- `commitChapter(message)`
- `createBranch(name, fromCommitId?)`
- `checkoutCommit(commitId)`（恢复快照）
- `diffCommits(a,b)`（只做“摘要 diff”：多少 panels/clips 改变、哪些 clip 参数变了）
- `exportCommitSnapshot(commitId)`（下载 JSON）

## 必加方法（批量队列）

- `startBatchRender(chapterId, options)`：
  - concurrency (1~3)
  - stopOnNeedsFix (true default)
- `pauseBatchRender(chapterId)`
- `resumeBatchRender(chapterId)`
- `cancelBatchRender(chapterId)`
- 批量渲染调度器：一次最多跑 concurrency 个 clip 的 videoJob
  - clip 进入 NeedsFix 则默认 stop（可配置）

## 必加方法（分析/发布）

- `recomputeAnalytics(chapterId)`：从 qaHistory/cost/budget 汇总
- `buildReleaseBundle(chapterId, releaseVersion)`：组装 ReleaseBundle
- `downloadReleaseBundle(chapterId)`

------

# C) MockWs 升级：Batch 调度事件 + Analytics 更新事件

修改：`src/lib/ws/mockWsServer.ts` 或 store 内部调度器即可。

新增 WS events（建议）：

- `batch_started/batch_progress/batch_paused/batch_resumed/batch_completed/batch_canceled`
- `analytics_updated`
- `release_ready`

> 第一版可以不用 WS，直接 store 内调度也行；但你系统一直强调统一事件流，建议还是 emit 事件。

------

# D) UI：新增四个“工业化控制台”入口

新增/修改：

```
src/components/studio/top/ChapterActions.tsx     // 顶部操作区：Template/Version/Batch/Analytics/Release
src/components/studio/modals/TemplateModal.tsx
src/components/studio/modals/VersionModal.tsx
src/components/studio/modals/BatchModal.tsx
src/components/studio/right/AnalyticsPanel.tsx  // 或 bottom tab
src/components/studio/modals/ReleaseModal.tsx
```

## 1) TemplateModal（模板）

- 左侧模板列表（内置几套模板：雨天治愈风、赛博朋克风…）
- 右侧模板详情：风格、角色包、场景包、QA/重试策略
- Apply scope：
  - Chapter defaults
  - All panels
  - All clips
  - Selected only（如果有选中 panels/clips）
- “Save current as template”

## 2) VersionModal（版本树）

- Commit list（时间线）
- Branch 下拉
- Compare 按钮（选择两个 commit 显示 diff 摘要）
- Checkout（恢复）
- Export snapshot 下载

## 3) BatchModal（批量）

- 显示 queue 列表（clipId + status + progress + provider + attempt）
- concurrency 选择（1~3）
- stopOnNeedsFix toggle
- Start/Pause/Resume/Cancel
- 统计：done/failed/needsFix

## 4) AnalyticsPanel（仪表盘）

- ChapterSummary 卡片（总成本、平均分、成功率）
- ByProvider 表格
- ByCharacter 表格
- “Recompute”按钮

## 5) ReleaseModal（发布交付）

- 输入 releaseVersion
- “Build release bundle”
- 完成后下载 `release_bundle.json`
- 显示包含哪些内容（exportSpec/qaReport/analytics）

------

# E) 导出协议：ReleaseBundle（商业化交付最关键）

你后续要对接外包/工作室/审核/投放，ReleaseBundle 就是“交付合同”。

**要包含：**

- export_spec（剪辑/合成依据）
- layerpack manifest 索引（可追溯）
- QA 报告（每段尝试）
- 成本（预算与消耗）
- 模板指纹（本章用的风格/角色/场景配置）

------

# F) 验收标准（必须全部满足）

1. 能把当前章保存成 template，并一键 apply 到 all clips ✅

2. commit 生成版本，checkout 后 timeline/绑定/QA配置恢复 ✅

3. 批量生成：一次跑多个 clip，出现 NeedsFix 会停止（默认） ✅

4. Analytics 页面能看到按 provider/角色资产统计 ✅

5. Release bundle 能下载，内容包含 exportSpec + qaReport + analytics ✅

6. 刷新页面：模板、版本树、批量队列状态（至少已完成的记录）仍可恢复 ✅

   

## 任务包10总目标

把你现在前端的“工厂控制台（mock闭环）”升级成“接上真实机器的工厂”：

- 前端仍负责：编排、参数、资产绑定、进度展示、人工介入
- 后端负责：持久化、队列调度、调用 ComfyUI/视频大模型、QA 评估、重试/回退、成本统计、导出交付包

------

# 1) 统一的“生产对象”与状态机（核心概念落地）

### 需要做什么

把所有生成过程抽象成 **Job**（任务），并定义稳定的状态机（前后端一致）：

**Job 类型**

- `image_job`：生成首尾帧/分层图（Flux/SD）
- `anchor_job`：生成控制图（depth/pose/seg…）
- `video_job`：首尾帧→视频（Kling/通义/豆包/ComfyUI）
- `export_job`：合成/导出（mp4、长条漫、release bundle）

**Job 状态**

- `QUEUED` → `RUNNING` → `SUCCEEDED`
- 失败：`FAILED`
- 需要修复：`NEEDS_FIX`
- 取消：`CANCELED`

**关键字段**

- `job_id, type, project_id, chapter_id, panel_id?, clip_id?`
- `provider, attempt, max_attempts`
- `progress(0-1), eta?, started_at, finished_at`
- `cost: { estimated, used }`
- `inputs(payload)`、`outputs(result)`
- `qa: { score, issues[] }`
- `error: { code, message, detail }`

> 你前端的 mock 状态机就是这个，只是现在要让后端成为“真源头”。

------

# 2) 数据库与持久化（后端必须做）

### 需要做什么

把你目前“前端草稿 + 本地存储”的关键数据落入数据库，至少支持：

- 多设备登录、协作
- Job 断点恢复
- 可追溯（QA/成本/尝试历史）

**建议最小表（Postgres）**

- `projects`
- `chapters`
- `panels`
- `assets`（统一存 identity/scene/keyframe/layerpack）
- `layerpacks`（每次渲染的输出 manifest）
- `clips`（timeline video track）
- `transitions`
- `bindings`（panel/clip -> identity/scene/anchor/style）
- `anchors`
- `jobs`
- `job_events`（可选：事件溯源/审计）
- `exports`（release bundle/ mp4/ strip）

**最小持久化策略**

- “章草稿”（chapter draft）存一份 JSON 快照（方便版本树）
- “生成结果”以 layerpack/export 的 manifest 持久化（可复现）

------

# 3) API：资源 CRUD（前端替换 mock 的第一步）

### 需要做什么

把前端当前用到的所有实体，提供标准 CRUD + 列表接口：

## Projects

- `GET /projects`
- `POST /projects`
- `GET /projects/{id}`
- `PATCH /projects/{id}`

## Chapters

- `GET /projects/{projectId}/chapters`
- `POST /projects/{projectId}/chapters`
- `GET /chapters/{id}`
- `PATCH /chapters/{id}`
- `POST /chapters/{id}/snapshot`（保存草稿快照，用于版本树）

## Panels

- `GET /chapters/{chapterId}/panels`
- `POST /chapters/{chapterId}/panels`
- `GET /panels/{id}`
- `PATCH /panels/{id}`

## Timeline / Clips

- `GET /chapters/{chapterId}/timeline`
- `PUT /chapters/{chapterId}/timeline`（整章覆盖保存，简单可靠）
- 或更细：`POST /clips` `PATCH /clips/{id}` `DELETE /clips/{id}`

## Assets（统一入口）

- `POST /assets/upload`（图片/safetensors/embedding）
- `GET /assets/{id}`
- `GET /assets?type=identity|scene|keyframe|layerpack...`

## Bindings（面板/片段绑定）

- `GET /chapters/{chapterId}/bindings`
- `PUT /chapters/{chapterId}/bindings`（整章保存）

> 这一步做完，前端“编辑器”就能真正变成多端可用的生产系统。

------

# 4) API：Job 创建与查询（真实生成入口）

### 需要做什么

把“生成动作”都变成创建 Job：

## 4.1 图像生成（首尾帧 / 分层）

- `POST /jobs/image`
  - 输入：panelId / clipId + prompt + styleProfile + identity assets + scene assets + seed + params
  - 输出：jobId
- `GET /jobs/{jobId}`（查询状态/进度/输出）

输出内容（成功后）

- `layerpack_id`
- `manifest_url`（MinIO）
- `images`: full/bg/char/lineart/alpha...（你 Flux 分层需要的）

## 4.2 Anchor 生成

- `POST /jobs/anchor`
  - 输入：panelId + kind(depth/pose/seg...) + sourceImageUrl
  - 输出：anchorId + jobId

成功后：anchor 记录落库，带 `control_image_url`

## 4.3 视频生成（双关键帧）

- `POST /jobs/video`
  - 输入：clipId + startFrame + endFrame + motionPrompt + duration + fps
  - 以及：styleProfile + identity assets + anchors + qaConfig + retryPolicy（或后端默认）
  - 输出：jobId

成功后：clip.output.videoUrl 写回、frame previews（可选）

## 4.4 导出/交付

- `POST /jobs/export`
  - 输入：chapterId + exportSpec（或后端从 DB 读）
  - 输出：jobId
- 成功后：export record（mp4、release_bundle.json）

------

# 5) WebSocket / SSE：事件推送（前端不轮询）

### 需要做什么

把后端执行进度、QA、重试、回退、成本等都通过事件推送给前端。

**推荐：WebSocket（你已有 ws.py）**

- `WS /ws?projectId=...`（鉴权后订阅）
- 事件统一结构：
  - `type`
  - `ts`
  - `jobId`
  - `chapterId/panelId/clipId`
  - `payload`

**必须支持的事件**

- `job_queued/job_started/job_progress/job_succeeded/job_failed/job_canceled`
- `qa_result`
- `retry_scheduled`
- `provider_fallback`
- `budget_updated`
- `anchor_generated`
- `layerpack_created`
- `export_ready`

> 前端继续沿用你现在的 `applyWsEvent`，只是事件源从 mock 换成后端 WS。

------

# 6) 队列系统（后端“工厂发动机”）

### 需要做什么

把 job 从 HTTP 请求剥离，交给后台队列执行，保证：

- 并发控制
- 失败重试
- 可恢复
- 不卡主 API

**建议最小实现**

- Redis + RQ / Celery（二选一）
- Worker 分类型：
  - `image-worker`
  - `anchor-worker`
  - `video-worker`
  - `export-worker`
- Job 执行过程：
  1. DB 标记 RUNNING
  2. 执行 provider 调用
  3. 存输出到 MinIO
  4. 更新 DB + 推送 WS 事件

------

# 7) Provider 适配层（把 ComfyUI / Kling / 通义 / 豆包统一起来）

### 需要做什么

后端实现 `ProviderAdapter` 统一接口：

**统一接口**

- `submit(payload) -> provider_job_id`
- `poll(provider_job_id) -> status/progress/result`
- `cancel(provider_job_id)`
- `estimate_cost(payload)`

**适配器类型**

- `ComfyUIAdapter`：HTTP 调用 ComfyUI prompt API（你已经有 client mock）
- `KlingAdapter`：HTTP API（异步任务+轮询）
- `TongyiVideoAdapter`
- `DoubaoVideoAdapter`

**关键点**

- 所有 adapter 最终都必须输出统一 result：
  - videoUrl / layerpack manifest / control image url
- provider 差异都在 adapter 内消化

------

# 8) QA 服务（质量判定与问题分类）

### 需要做什么

把你在前端 mock 的 QA 变成后端可配置服务（先简单、后升级）：

**最小 QA：规则+模型结合**

- 图像：清晰度、噪点、NSFW/敏感（必要时）、文本可读性
- 角色一致性：FaceID embedding 相似度阈值（>=0.75）
- 视频：闪烁检测（frame-to-frame diff）、人物漂移、画风漂移

**输出**

- `score: 0..1`
- `issues[]`（结构化，便于自动修复策略选择）
  - `IDENTITY_DRIFT`
  - `FLICKER`
  - `ANATOMY_BAD`
  - `BACKGROUND_SHIFT`
  - `TEXT_UNREADABLE`
  - `ARTIFACTS`

------

# 9) 自动重试与回退（真正的商业化稳定器）

### 需要做什么

把任务包08的“自动重试逻辑”搬到后端队列：

**重试触发**

- `qa.score < threshold` 或出现关键 issues

**重试策略**

- attempts < maxAttempts
- budget 未超
- providerChain 下一位
- 参数微调（例如：
  - 提升 identity strength
  - 增加 anchor weight
  - 调整 motion_bucket / cfg / steps）

**回退**

- provider A 不稳定 → fallback 到 B
- 并记录 `qaHistory` 与 `costHistory`

**最终产物**

- job 在 DB 中保留尝试历史（审计/统计/成本）

------

# 10) 成本核算与统计（仪表盘真实数据源）

### 需要做什么

后端对每次 job 执行写入：

- `cost_used`
- `provider`
- `duration_ms`
- `attempt`
- `qa_score`

并提供统计接口：

- `GET /chapters/{id}/analytics`
- `GET /projects/{id}/analytics`

这样任务包09的仪表盘就能用真实数据。

------

# 11) Release Bundle（交付包）在后端生成

### 需要做什么

把 release bundle 的生成放后端，确保：

- 数据来自 DB（权威）
- 资源 URL 都是可用的（MinIO）
- 版本可追溯

接口：

- `POST /chapters/{id}/release`（输入 version、notes）

- 输出：release record + bundle_url

  ### 验收清单：

  



# 任务包A01：生产规格与文件协议定版（先定规矩）

### 目标

全系统统一“画幅、命名、目录、manifest结构”，否则后面越做越乱。

### 要做什么

- 定义**面板基准规格**（建议先固定一个：1080×1920 或 1024×1536）
- 定义**LayerPack 最小输出**：`full.png` + `manifest.json`
- 定义 MinIO 目录规范（按 project/chapter/panel/attempt）
- 定义 manifest 最小字段：ids、尺寸、seed、模型信息、outputs路径、createdAt

### 产出物

- `LayerPack Manifest v1` 文档（可放到 repo /docs）
- MinIO key 模板（字符串规则）

### 验收标准

- 任意一个 panel 的产物目录“拿到就能复现/追溯”（至少能看到参数与输出路径）

### 归属

- **前后端结合**（规范由你定，后端落地为主，前端展示为辅）

------

# 任务包A02：后端权威数据落地（DB + CRUD 完整闭环）

### 目标

前端不再靠本地草稿当真相，DB 才是权威。

### 要做什么

- projects/chapters/panels/timeline 的 CRUD 与关联字段补齐
- chapter/panel 的排序字段固定（order/index），避免长条漫顺序漂
- timeline 先做“整章覆盖保存”最稳（PUT timeline）

### 产出物

- 数据库迁移（tables/indices）
- OpenAPI 中可用的 CRUD 接口

### 验收标准

- 新建章→加10个panel→保存→刷新页面→顺序/内容完全一致
- timeline 保存后，二次进入仍一致

### 归属

- **后端为主**，前端改数据源

------

# 任务包A03：Job 系统 + Worker + WS 事件流（生产发动机）

### 目标

所有生成行为都走 job：可排队、可恢复、可追踪、可重试。

### 要做什么

- 设计统一 Job 模型（image_job 是第一优先）
- 建立队列执行（Redis+RQ/Celery 任一）
- WS 推送 job 状态：queued/running/progress/succeeded/failed/canceled
- 前端用同一套事件 reducer 更新 UI（你现在 mock 的那套）

### 产出物

- jobs 表 + worker 进程
- WS 事件协议（后端发、前端收）

### 验收标准

- 前端点“生成面板图”→立即拿到 jobId
- UI 实时显示 RUNNING / 进度 / 完成
- 断开重连后还能恢复正确状态（至少 GET job 能查到）

### 归属

- **前后端结合**（后端实现为主，前端接收渲染）

------

# 任务包A04：ComfyUI 静态图生成接入（Flux 单图先跑通）

### 目标

把你现在 Flux 能跑的 workflow 变成后端可调用的生产能力。

### 要做什么

- 后端实现 ComfyUIAdapter（submit/poll/cancel）
- 固化一套 Flux 生成 workflow 模板（prompt/seed/尺寸/steps/CFG 可注入）
- 产物落 MinIO：至少 full.png
- Job outputs 写回：image_url、seed、参数、耗时

### 产出物

- `/jobs/image` 真正生成 Flux 图片
- 一套 workflow template（JSON + 参数注入器）

### 验收标准

- 给一个 panel prompt，能稳定生成 full 图并在前端显示
- 失败能返回可读 error（不是“静默卡住”）

### 归属

- **后端为主**（adapter + 产物落地），前端负责展示

------

# 任务包A05：LayerPack v1（把“图片”升级成“可交付资产包”）

### 目标

每次生成不只是图，而是“可追溯的交付包（manifest+outputs）”。

### 要做什么

- 每个 image_job 成功后创建 layerpack 记录
- 生成 manifest.json 并上传 MinIO（跟 full 同目录）
- DB 保存 layerpack 索引：panelId、attempt、manifestUrl、fullUrl、params摘要
- 前端 panel 详情页能选择“当前使用的 layerpack”（默认最新成功）

### 产出物

- layerpacks 表 + manifest 生成器
- 前端面板：LayerPack 列表/切换

### 验收标准

- 同一 panel 生成 3 次：前端能切换不同 layerpack
- 导出时能准确引用选中的那个

### 归属

- **前后端结合**（后端落地、前端管理与展示）

------

# 任务包A06：Flux 静态图“分层”（商业化关键：后期可编辑）

> 这一包你可以做成 **v1.0 最小分层**：先做到 “full + char_mask”，后面再扩展 bg/line/alpha。

### 目标

给后期编辑/换背景/加动效留接口，提升商业化可控性。

### 要做什么

- 在 ComfyUI 或后端加一道分割：输出 `char_mask.png`
   （先用最稳的方案：SAM/人物分割/抠图模型，别一上来追求完美）
- 把 `char_mask` 也写入 LayerPack outputs
- 可选 v1.1：用 mask 做 background inpaint 得到 `bg.png`

### 产出物

- LayerPack outputs 至少包含：
  - `full.png`
  - `char_mask.png`
- （可选）`bg.png`

### 验收标准

- 你能在前端预览：full 与 mask
- mask 尺寸与 full 一致（这是硬标准）

### 归属

- **后端为主**（生成与存储），前端负责可视化

------

# 任务包A07：气泡排版与文本渲染对接（可卖的“成品感”）

### 目标

从“好看的图”升级到“能读的漫画”。

### 要做什么

- 前端 BubbleEditor 的数据结构定稿（气泡、文本、字体、位置、层级）
- 后端 typesetter 服务接收：底图(full) + bubbles JSON → 输出带字成品 panel.png
- 产物作为 LayerPack 的一个变体：`final_typeset.png`（推荐）

### 产出物

- typeset API 可用：`POST /panels/{id}/typeset`
- LayerPack 记录 typeset 输出

### 验收标准

- 同一 panel：改文字→重新 typeset→导出成品可读
- 字体/换行/描边等能满足“移动端阅读”

### 归属

- **前后端结合**（前端编辑、后端渲染）

------

# 任务包A08：章节导出（长条漫 strip + 交付包 release_bundle）

### 目标

一键导出“可交付成品 + 可追溯素材包”，这就是你商业化第一阶段的交付。

### 要做什么

- export_job：按 panel 顺序读取每个 panel 的最终图（优先 typeset，其次 full）
- 拼接为长条漫 strip（png/webp）
- 生成 release_bundle.json（最小版即可）包含：
  - chapter信息、panel顺序
  - 每个 panel 的 layerpack manifestUrl
  - 导出文件 URL
  - 成本与参数摘要（先简单统计）

### 产出物

- `/jobs/export` 返回 stripUrl + bundleUrl
- 前端“导出”按钮 + 下载入口

### 验收标准

- 一章 20 个 panel：一键导出成功
- bundle 里能追溯每个 panel 用的 layerpack

### 归属

- **后端为主**（合成与打包），前端负责触发与下载

------

# 任务包A09：最小 QA（静态可控性：拒绝明显坏图）

### 目标

降低返工，提升“稳定可交付”。

### 要做什么

- 生成后做最小 QA：
  - 分辨率一致性
  - 黑图/空图检测
  - 过曝/模糊（简单阈值）
  - （可选）NSFW 安全过滤（取决于你的商业方向）
- QA 不过：job 进入 NEEDS_FIX，并返回原因

### 产出物

- qa_report 写入 job.outputs（结构化 issues）

### 验收标准

- 故意给异常输入能被拦住，并提示“哪里不行”

### 归属

- **后端为主**，前端展示 QA 结果

------

# 任务包A10：模板化（风格包/角色包/场景包）用于规模化连载

### 目标

做“系列化、连载化、工作室化”。

### 要做什么

- 模板实体：styleProfile + identityAssets + sceneAssets + 默认渲染参数
- apply template 到章节/所有 panel（批量更新 bindings）
- 前端能选择模板并一键应用

### 产出物

- templates 表 + apply 接口
- 前端 Template 面板

### 验收标准

- 换一个模板，整章风格与参数一键切换（至少影响下一次生成）

### 归属

- **前后端结合**

。